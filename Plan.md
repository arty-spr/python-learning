# 🐍 31-дневный план изучения Python

## Формат обучения

| Параметр | Значение |
|----------|----------|
| Время | 9:00 — 14:00 (5 часов чистого времени) |
| Перерывы | 15 минут после каждого часа |
| Структура | Дни 1-6: теория + практика, День 7: повторение + сложная задача |

### Расписание дня
```
09:00 — 10:00  Блок 1
10:00 — 10:15  ☕ Перерыв
10:15 — 11:15  Блок 2
11:15 — 11:30  ☕ Перерыв
11:30 — 12:30  Блок 3
12:30 — 12:45  ☕ Перерыв
12:45 — 13:45  Блок 4
13:45 — 14:00  Итоги дня
```

---

## 📚 Основные ресурсы

### Тренажёры (бесплатные)
| Ресурс | Описание | Ссылка |
|--------|----------|--------|
| Stepik | «Поколение Python» — лучший курс на русском | stepik.org/course/58852 |
| Codewars | Задачи по уровням (8 kyu → 1 kyu) | codewars.com |
| LeetCode | Алгоритмические задачи | leetcode.com |
| Python Tutor | Визуализация выполнения кода | pythontutor.com |

### Документация
- docs.python.org — официальная документация
- realpython.com — статьи и туториалы

---

# НЕДЕЛЯ 1: Основы синтаксиса

> **Цель недели:** Освоить базовый синтаксис, научиться думать алгоритмически

---

## 📅 День 1 — Введение и переменные

### Блок 1 (9:00–10:00) | Теория: Знакомство с Python
- [ ] Что такое Python, история, где применяется
- [ ] Установка Python 3.12+ (python.org)
- [ ] Установка VS Code + расширение Python
- [ ] Настройка: создать папку `python_learning`, первый файл `day1.py`
- [ ] REPL: открыть терминал, написать `python`, поиграть с командами
- [ ] Первая программа: `print("Привет, мир!")`

### Блок 2 (10:15–11:15) | Теория: Переменные и типы данных
- [ ] Что такое переменная (коробка с именем)
- [ ] Правила именования: `snake_case`, нельзя начинать с цифры
- [ ] Типы данных:
  - `int` — целые числа: `age = 25`
  - `float` — дробные: `price = 19.99`
  - `str` — строки: `name = "Алекс"`
  - `bool` — логические: `is_active = True`
- [ ] Функция `type()` — проверка типа
- [ ] Динамическая типизация: переменная может менять тип

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Зарегистрироваться на Stepik
- [ ] Начать курс «Поколение Python для начинающих»
- [ ] Пройти модуль 1: Ввод-вывод данных (все задачи)
- [ ] Пройти модуль 2: Переменные (начать)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Создать файл `day1_practice.py` и написать:

```python
# Задача 1: Визитка
# Создай переменные: имя, возраст, город, профессия
# Выведи красивую визитку

# Задача 2: Калькулятор возраста
# Попроси пользователя ввести год рождения
# Посчитай и выведи возраст (2025 - год)

# Задача 3: Конвертер валют
# Курс: 1 USD = 87 RUB
# Попроси ввести сумму в долларах, выведи в рублях

# Задача 4: Данные пользователя
# Попроси ввести имя, рост (в см), вес
# Выведи все данные + тип каждой переменной
```

### 13:45–14:00 | Итоги дня
- [ ] Записать в заметки: что такое переменная, 4 базовых типа
- [ ] Отметить вопросы, если есть

---

## 📅 День 2 — Операторы и выражения

### Блок 1 (9:00–10:00) | Теория: Арифметические операторы
- [ ] Основные операторы:
  ```python
  +   # сложение: 5 + 3 = 8
  -   # вычитание: 5 - 3 = 2
  *   # умножение: 5 * 3 = 15
  /   # деление: 5 / 3 = 1.666...
  //  # целочисленное деление: 5 // 3 = 1
  %   # остаток от деления: 5 % 3 = 2
  **  # возведение в степень: 5 ** 3 = 125
  ```
- [ ] Приоритет операций: `**` → `* / // %` → `+ -`
- [ ] Скобки меняют приоритет: `(2 + 3) * 4 = 20`

### Блок 2 (10:15–11:15) | Теория: Сравнение и логика
- [ ] Операторы сравнения (возвращают `bool`):
  ```python
  ==  # равно
  !=  # не равно
  >   # больше
  <   # меньше
  >=  # больше или равно
  <=  # меньше или равно
  ```
- [ ] Логические операторы:
  ```python
  and  # И: True and False = False
  or   # ИЛИ: True or False = True
  not  # НЕ: not True = False
  ```
- [ ] Комбинирование: `age >= 18 and has_passport == True`

### Блок 3 (11:30–12:30) | Практика: Stepik + Codewars
- [ ] Stepik: модуль про операторы (все задачи)
- [ ] Codewars: зарегистрироваться, выбрать Python
- [ ] Решить 5 задач уровня 8 kyu:
  - "Multiply"
  - "Even or Odd"
  - "Sum of positive"
  - "Opposite number"
  - "Return Negative"

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day2_practice.py`:

```python
# Задача 1: Чётность
# Введи число, определи чётное оно или нет (% 2)

# Задача 2: Делимость
# Проверь, делится ли число на 3 И на 5 одновременно

# Задача 3: Калькулятор ИМТ
# Формула: вес / (рост_в_метрах ** 2)
# Попроси ввести вес (кг) и рост (см), выведи ИМТ

# Задача 4: Сумма цифр двузначного числа
# Введи двузначное число, выведи сумму его цифр
# Подсказка: 47 // 10 = 4, 47 % 10 = 7

# Задача 5: Время
# Введи количество секунд, переведи в часы:минуты:секунды
```

### 13:45–14:00 | Итоги дня
- [ ] Создать шпаргалку по всем операторам
- [ ] Запомнить: `//` и `%` — очень полезны!

---

## 📅 День 3 — Условные конструкции

### Блок 1 (9:00–10:00) | Теория: if, else, elif
- [ ] Базовая конструкция:
  ```python
  if условие:
      # код выполнится, если условие True
  ```
- [ ] if-else:
  ```python
  if возраст >= 18:
      print("Доступ разрешён")
  else:
      print("Доступ запрещён")
  ```
- [ ] if-elif-else (множественный выбор):
  ```python
  if оценка >= 90:
      print("Отлично")
  elif оценка >= 70:
      print("Хорошо")
  elif оценка >= 50:
      print("Удовлетворительно")
  else:
      print("Неудовлетворительно")
  ```
- [ ] **КРИТИЧНО:** Отступы в Python = 4 пробела!

### Блок 2 (10:15–11:15) | Теория: Продвинутые условия
- [ ] Вложенные условия:
  ```python
  if has_ticket:
      if age >= 18:
          print("Проходите")
      else:
          print("Нужен взрослый")
  else:
      print("Купите билет")
  ```
- [ ] Тернарный оператор:
  ```python
  статус = "взрослый" if age >= 18 else "ребёнок"
  ```
- [ ] Truthy и Falsy значения:
  - Falsy: `0`, `""`, `None`, `[]`, `False`
  - Truthy: всё остальное
- [ ] Цепочки сравнений: `18 <= age <= 65`

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль условных операторов (все задачи)
- [ ] Особое внимание на задачи с вложенными условиями

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day3_practice.py`:

```python
# Задача 1: Время года
# Введи номер месяца (1-12), выведи время года

# Задача 2: Високосный год
# Год високосный, если:
# - делится на 4 И не делится на 100
# - ИЛИ делится на 400

# Задача 3: Калькулятор
# Введи два числа и операцию (+, -, *, /)
# Выполни операцию, выведи результат
# Обработай деление на ноль!

# Задача 4: Квадрант координат
# Введи x и y, определи квадрант (I, II, III, IV) или ось

# Задача 5: Тариф мобильной связи
# До 100 минут: 0.5 руб/мин
# 100-300 минут: 0.3 руб/мин
# Более 300: 0.1 руб/мин
# Посчитай стоимость по введённым минутам
```

### 13:45–14:00 | Итоги дня
- [ ] Нарисовать блок-схему для задачи про тариф
- [ ] Запомнить: всегда думай о крайних случаях!

---

## 📅 День 4 — Цикл while

### Блок 1 (9:00–10:00) | Теория: Основы while
- [ ] Зачем нужны циклы (повторение без копирования кода)
- [ ] Синтаксис:
  ```python
  while условие:
      # код выполняется, пока условие True
  ```
- [ ] Пример — счётчик:
  ```python
  count = 1
  while count <= 5:
      print(count)
      count += 1  # БЕЗ ЭТОГО — бесконечный цикл!
  ```
- [ ] Бесконечный цикл: `while True:` (осторожно!)

### Блок 2 (10:15–11:15) | Теория: Управление циклом
- [ ] `break` — немедленный выход из цикла:
  ```python
  while True:
      ответ = input("Выход? (да/нет): ")
      if ответ == "да":
          break
  ```
- [ ] `continue` — пропустить итерацию:
  ```python
  i = 0
  while i < 10:
      i += 1
      if i % 2 == 0:
          continue  # пропускаем чётные
      print(i)
  ```
- [ ] `while-else` — выполнится, если цикл завершился без break
- [ ] Паттерн «флаг»:
  ```python
  found = False
  while not found:
      # ищем что-то
      if нашли:
          found = True
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik + визуализация
- [ ] Stepik: модуль циклов while
- [ ] Python Tutor: вставить 3 своих цикла, посмотреть пошаговое выполнение
- [ ] Codewars: 3 задачи на while (8-7 kyu)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day4_practice.py`:

```python
# Задача 1: Угадай число
# Компьютер загадывает число от 1 до 100
# Пользователь угадывает, программа говорит "больше"/"меньше"
# Подсказка: import random; число = random.randint(1, 100)

# Задача 2: Сумма цифр
# Введи число, посчитай сумму всех его цифр
# 12345 → 1+2+3+4+5 = 15

# Задача 3: Числа Фибоначчи
# Выведи все числа Фибоначчи, меньшие N
# 1, 1, 2, 3, 5, 8, 13, 21...

# Задача 4: Проверка простоты
# Введи число, определи простое оно или нет
# Простое — делится только на 1 и себя

# Задача 5: Банкомат
# Сумма на счету: 10000
# Пользователь снимает деньги, пока не введёт 0 или не кончатся
# Выводи остаток после каждой операции
```

### 13:45–14:00 | Итоги дня
- [ ] Записать: 3 способа выйти из while
- [ ] Главная ошибка — забыть изменить переменную условия!

---

## 📅 День 5 — Цикл for

### Блок 1 (9:00–10:00) | Теория: Основы for
- [ ] Синтаксис:
  ```python
  for элемент in последовательность:
      # код для каждого элемента
  ```
- [ ] Функция `range()`:
  ```python
  range(5)        # 0, 1, 2, 3, 4
  range(1, 6)     # 1, 2, 3, 4, 5
  range(0, 10, 2) # 0, 2, 4, 6, 8
  range(10, 0, -1) # 10, 9, 8... 1
  ```
- [ ] Итерация по строке:
  ```python
  for char in "Python":
      print(char)  # P, y, t, h, o, n
  ```

### Блок 2 (10:15–11:15) | Теория: Продвинутый for
- [ ] Вложенные циклы:
  ```python
  for i in range(3):
      for j in range(3):
          print(f"({i}, {j})")
  ```
- [ ] `break` и `continue` работают так же, как в while
- [ ] `enumerate()` — получить индекс и значение:
  ```python
  for index, char in enumerate("abc"):
      print(f"{index}: {char}")
  ```
- [ ] Когда `for`, когда `while`:
  - `for` — знаем количество итераций
  - `while` — не знаем, ждём условия

### Блок 3 (11:30–12:30) | Практика: Stepik + Codewars
- [ ] Stepik: модуль циклов for (все задачи)
- [ ] Codewars: 5 задач на циклы (8-7 kyu)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day5_practice.py`:

```python
# Задача 1: Таблица умножения
# Выведи таблицу умножения для числа N (от 1 до 10)

# Задача 2: Треугольник
# Введи N, выведи треугольник:
# *
# **
# ***
# ****

# Задача 3: Факториал
# N! = 1 * 2 * 3 * ... * N
# Введи N, вычисли факториал

# Задача 4: Все делители
# Введи число, выведи все его делители

# Задача 5: Шахматная доска
# Выведи доску 8x8:
# * * * *
#  * * * *
# * * * *
# ...

# Задача 6: Простые числа
# Выведи все простые числа от 2 до N
```

### 13:45–14:00 | Итоги дня
- [ ] Таблица: for vs while — когда что использовать
- [ ] Вложенные циклы — внешний меняется медленно, внутренний быстро

---

## 📅 День 6 — Строки

### Блок 1 (9:00–10:00) | Теория: Строки как последовательности
- [ ] Строка — последовательность символов
- [ ] Индексация:
  ```python
  s = "Python"
  s[0]   # 'P' — первый символ
  s[-1]  # 'n' — последний символ
  s[2]   # 't'
  ```
- [ ] Срезы (slices):
  ```python
  s[0:3]   # 'Pyt' — с 0 по 2 (не включая 3)
  s[2:]    # 'thon' — с 2 до конца
  s[:3]    # 'Pyt' — с начала до 2
  s[::2]   # 'Pto' — каждый второй
  s[::-1]  # 'nohtyP' — реверс строки
  ```
- [ ] Строки неизменяемы! `s[0] = 'J'` — ошибка

### Блок 2 (10:15–11:15) | Теория: Методы строк
- [ ] Изменение регистра:
  ```python
  s.upper()      # 'PYTHON'
  s.lower()      # 'python'
  s.capitalize() # 'Python'
  s.title()      # 'Hello World'
  ```
- [ ] Поиск и проверка:
  ```python
  s.find('th')      # 2 (индекс) или -1
  s.count('t')      # 1
  s.startswith('Py') # True
  s.endswith('on')   # True
  s.isdigit()       # только цифры?
  s.isalpha()       # только буквы?
  ```
- [ ] Преобразование:
  ```python
  s.strip()         # убрать пробелы по краям
  s.replace('P', 'J') # 'Jython'
  s.split(',')      # разбить в список
  ','.join(['a','b']) # 'a,b'
  ```
- [ ] f-строки (форматирование):
  ```python
  name = "Мир"
  f"Привет, {name}!"  # 'Привет, Мир!'
  f"2 + 2 = {2 + 2}"  # '2 + 2 = 4'
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль про строки (все задачи)
- [ ] Особое внимание на срезы и методы

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day6_practice.py`:

```python
# Задача 1: Палиндром
# Проверь, является ли строка палиндромом
# "казак" → да, "привет" → нет

# Задача 2: Подсчёт слов
# Введи предложение, посчитай количество слов

# Задача 3: Замена гласных
# Замени все гласные в строке на '*'

# Задача 4: Анаграмма
# Проверь, являются ли две строки анаграммами
# "кот" и "ток" → да

# Задача 5: Форматирование имени
# Введи "иВаНоВ иВаН иВаНоВиЧ"
# Выведи "Иванов Иван Иванович"

# Задача 6: Шифр Цезаря (простой)
# Сдвинь каждую букву на N позиций
# 'abc', сдвиг 1 → 'bcd'
```

### 13:45–14:00 | Итоги дня
- [ ] Шпаргалка: 10 самых полезных методов строк
- [ ] Запомнить: строки неизменяемы, методы возвращают новую строку

---

## 📅 День 7 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Переменные и операторы
- [ ] Пройтись по шпаргалкам дней 1-2
- [ ] Быстрый тест: написать 10 выражений с разными операторами
- [ ] Проверить себя: что вернёт `17 // 5`, `17 % 5`, `2 ** 10`?

### Блок 2 (10:15–11:15) | Повторение: Условия и циклы
- [ ] Пройтись по шпаргалкам дней 3-5
- [ ] Написать по памяти: if-elif-else, while с break, for с range
- [ ] Тест: когда использовать while, когда for?

### Блок 3 (11:30–12:30) | Повторение: Строки
- [ ] Шпаргалка дня 6
- [ ] Написать 5 примеров срезов
- [ ] Вспомнить 10 методов строк

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 1

## Задача: «Консольный валидатор паролей»

Напиши программу проверки надёжности пароля.

**Требования к паролю:**
1. Минимум 8 символов
2. Минимум 1 заглавная буква
3. Минимум 1 строчная буква
4. Минимум 1 цифра
5. Минимум 1 специальный символ: `!@#$%^&*`

**Логика программы:**
1. Программа просит ввести пароль
2. Проверяет ВСЕ требования
3. Выводит, какие требования выполнены ✓, какие нет ✗
4. Выводит оценку: Слабый / Средний / Сильный / Очень сильный
   - 1-2 требования: Слабый
   - 3 требования: Средний
   - 4 требования: Сильный
   - 5 требований: Очень сильный
5. Предлагает ввести новый пароль или выйти (цикл!)

**Пример вывода:**
```
Введите пароль: Hello123

Проверка пароля:
✓ Длина >= 8 символов
✓ Есть заглавная буква
✓ Есть строчная буква
✓ Есть цифра
✗ Нет специального символа (!@#$%^&*)

Оценка: Сильный (4/5)

Ввести новый пароль? (да/нет):
```

**Подсказки:**
- Используй цикл for для проверки каждого символа
- Методы: `isupper()`, `islower()`, `isdigit()`
- Для спецсимволов: `char in "!@#$%^&*"`
- Счётчик выполненных требований для оценки

---

# НЕДЕЛЯ 2: Структуры данных и функции

> **Цель недели:** Освоить коллекции (списки, словари) и научиться писать функции

---

## 📅 День 8 — Списки: основы

### Блок 1 (9:00–10:00) | Теория: Создание и индексация
- [ ] Что такое список (изменяемая последовательность):
  ```python
  numbers = [1, 2, 3, 4, 5]
  mixed = [1, "hello", 3.14, True]
  empty = []
  ```
- [ ] Индексация (как в строках):
  ```python
  numbers[0]   # 1
  numbers[-1]  # 5
  numbers[1:3] # [2, 3]
  ```
- [ ] Списки изменяемы!
  ```python
  numbers[0] = 100  # Работает!
  ```
- [ ] `len()` — длина списка

### Блок 2 (10:15–11:15) | Теория: Методы списков
- [ ] Добавление:
  ```python
  lst.append(x)     # добавить в конец
  lst.insert(i, x)  # вставить на позицию i
  lst.extend([1,2]) # добавить несколько
  ```
- [ ] Удаление:
  ```python
  lst.remove(x)  # удалить первое вхождение x
  lst.pop()      # удалить и вернуть последний
  lst.pop(i)     # удалить по индексу
  del lst[i]     # удалить по индексу
  lst.clear()    # очистить
  ```
- [ ] Поиск и сортировка:
  ```python
  lst.index(x)   # индекс элемента
  lst.count(x)   # количество вхождений
  lst.sort()     # отсортировать (изменяет список!)
  lst.reverse()  # перевернуть
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль списков — часть 1 (все задачи)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day8_practice.py`:

```python
# Задача 1: Среднее значение
# Введи числа через пробел, найди среднее
# Подсказка: input().split() даёт список строк

# Задача 2: Максимум и минимум
# Найди макс и мин БЕЗ встроенных функций max/min

# Задача 3: Удаление дубликатов
# Создай новый список без повторяющихся элементов
# [1, 2, 2, 3, 3, 3] → [1, 2, 3]

# Задача 4: Реверс списка
# Переверни список БЕЗ метода reverse и среза [::-1]

# Задача 5: Слияние отсортированных
# Два отсортированных списка → один отсортированный
# [1, 3, 5] + [2, 4, 6] → [1, 2, 3, 4, 5, 6]
```

### 13:45–14:00 | Итоги дня
- [ ] Шпаргалка: методы списков
- [ ] Разница: `append` vs `extend`, `remove` vs `pop`

---

## 📅 День 9 — Списки: продвинутые техники

### Блок 1 (9:00–10:00) | Теория: List comprehension
- [ ] Генераторы списков (list comprehension):
  ```python
  # Вместо:
  squares = []
  for x in range(10):
      squares.append(x ** 2)
  
  # Пишем:
  squares = [x ** 2 for x in range(10)]
  ```
- [ ] С условием:
  ```python
  evens = [x for x in range(20) if x % 2 == 0]
  ```
- [ ] С преобразованием:
  ```python
  words = ["hello", "world"]
  upper = [w.upper() for w in words]
  ```

### Блок 2 (10:15–11:15) | Теория: Вложенные списки и полезные функции
- [ ] Вложенные списки (матрицы):
  ```python
  matrix = [
      [1, 2, 3],
      [4, 5, 6],
      [7, 8, 9]
  ]
  matrix[0][1]  # 2
  ```
- [ ] Полезные функции:
  ```python
  sum(lst)       # сумма
  min(lst)       # минимум
  max(lst)       # максимум
  sorted(lst)    # новый отсортированный (не изменяет!)
  list(range(5)) # [0, 1, 2, 3, 4]
  ```
- [ ] `zip()` — объединение списков:
  ```python
  names = ["Аня", "Боря"]
  ages = [25, 30]
  list(zip(names, ages))  # [("Аня", 25), ("Боря", 30)]
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik + Codewars
- [ ] Stepik: модуль списков — часть 2
- [ ] Codewars: 5 задач на списки (7 kyu)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day9_practice.py`:

```python
# Задача 1: Квадраты нечётных
# Одной строкой: список квадратов нечётных чисел от 1 до 20

# Задача 2: Фильтрация слов
# Из списка слов оставь только те, что длиннее 5 букв

# Задача 3: Транспонирование матрицы
# [[1,2,3], [4,5,6]] → [[1,4], [2,5], [3,6]]

# Задача 4: Сглаживание списка
# [[1,2], [3,4], [5,6]] → [1,2,3,4,5,6]

# Задача 5: Общие элементы
# Найди элементы, которые есть в обоих списках
```

### 13:45–14:00 | Итоги дня
- [ ] Практика: переписать 3 старые задачи с list comprehension
- [ ] Запомнить синтаксис: `[выражение for x in итерируемое if условие]`

---

## 📅 День 10 — Кортежи и множества

### Блок 1 (9:00–10:00) | Теория: Кортежи (tuple)
- [ ] Кортеж — неизменяемый список:
  ```python
  point = (10, 20)
  rgb = (255, 128, 0)
  single = (42,)  # запятая обязательна!
  ```
- [ ] Зачем нужны:
  - Защита от изменений
  - Ключи словарей (списки нельзя)
  - Распаковка: `x, y = point`
  - Возврат нескольких значений из функции
- [ ] Методы: только `count()` и `index()`

### Блок 2 (10:15–11:15) | Теория: Множества (set)
- [ ] Множество — неупорядоченная коллекция уникальных элементов:
  ```python
  s = {1, 2, 3}
  s = set([1, 2, 2, 3])  # {1, 2, 3}
  empty = set()  # НЕ {} — это словарь!
  ```
- [ ] Операции:
  ```python
  s.add(4)       # добавить
  s.remove(2)    # удалить (ошибка если нет)
  s.discard(2)   # удалить (без ошибки)
  ```
- [ ] Математика множеств:
  ```python
  a | b  # объединение
  a & b  # пересечение
  a - b  # разность
  a ^ b  # симметрическая разность
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модули про кортежи и множества

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day10_practice.py`:

```python
# Задача 1: Распаковка координат
# Список точек: [(1,2), (3,4), (5,6)]
# Выведи "Точка: x=1, y=2" для каждой

# Задача 2: Уникальные слова
# Введи текст, выведи количество уникальных слов

# Задача 3: Общие друзья
# Два множества друзей — найди общих

# Задача 4: Удаление дубликатов с сохранением порядка
# [1, 2, 2, 3, 1, 4] → [1, 2, 3, 4]
# Подсказка: множество для проверки, список для результата

# Задача 5: Анализ текста
# Введи два предложения
# Выведи: общие слова, уникальные для первого, уникальные для второго
```

### 13:45–14:00 | Итоги дня
- [ ] Таблица: список vs кортеж vs множество
- [ ] Когда что использовать?

---

## 📅 День 11 — Словари

### Блок 1 (9:00–10:00) | Теория: Основы словарей
- [ ] Словарь — коллекция пар ключ: значение:
  ```python
  person = {
      "name": "Иван",
      "age": 25,
      "city": "Москва"
  }
  ```
- [ ] Доступ к значениям:
  ```python
  person["name"]      # "Иван"
  person.get("name")  # "Иван"
  person.get("job", "Не указано")  # значение по умолчанию
  ```
- [ ] Изменение:
  ```python
  person["age"] = 26           # изменить
  person["job"] = "Разработчик" # добавить
  del person["city"]           # удалить
  ```

### Блок 2 (10:15–11:15) | Теория: Методы и итерация
- [ ] Методы:
  ```python
  d.keys()    # все ключи
  d.values()  # все значения
  d.items()   # пары (ключ, значение)
  d.update(other_dict)  # объединить
  d.pop("key")          # удалить и вернуть
  ```
- [ ] Итерация:
  ```python
  for key in person:
      print(key, person[key])
  
  for key, value in person.items():
      print(f"{key}: {value}")
  ```
- [ ] Проверка наличия ключа:
  ```python
  if "name" in person:
      print("Ключ есть")
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль словарей (все задачи)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day11_practice.py`:

```python
# Задача 1: Подсчёт символов
# Введи строку, создай словарь {символ: количество}

# Задача 2: Телефонная книга
# Реализуй: добавление, поиск, удаление контактов
# Цикл с меню

# Задача 3: Инвертирование словаря
# {"a": 1, "b": 2} → {1: "a", 2: "b"}

# Задача 4: Группировка по первой букве
# ["apple", "banana", "apricot", "blueberry"]
# → {"a": ["apple", "apricot"], "b": ["banana", "blueberry"]}

# Задача 5: Слияние словарей с суммированием
# {"a": 1, "b": 2} + {"b": 3, "c": 4} → {"a": 1, "b": 5, "c": 4}
```

### 13:45–14:00 | Итоги дня
- [ ] Шпаргалка: методы словарей
- [ ] Словари — самая важная структура для работы с данными!

---

## 📅 День 12 — Функции: основы

### Блок 1 (9:00–10:00) | Теория: Создание функций
- [ ] Зачем нужны функции (DRY — Don't Repeat Yourself):
  ```python
  def greet(name):
      """Приветствует пользователя."""
      print(f"Привет, {name}!")
  
  greet("Мир")  # Привет, Мир!
  ```
- [ ] Возврат значения:
  ```python
  def add(a, b):
      return a + b
  
  result = add(2, 3)  # 5
  ```
- [ ] Без return функция возвращает `None`
- [ ] Docstring — документация функции

### Блок 2 (10:15–11:15) | Теория: Аргументы
- [ ] Позиционные и именованные:
  ```python
  def power(base, exp):
      return base ** exp
  
  power(2, 3)        # позиционные
  power(exp=3, base=2)  # именованные
  ```
- [ ] Значения по умолчанию:
  ```python
  def greet(name, greeting="Привет"):
      print(f"{greeting}, {name}!")
  
  greet("Мир")           # Привет, Мир!
  greet("Мир", "Здравствуй")  # Здравствуй, Мир!
  ```
- [ ] `*args` — произвольное число аргументов:
  ```python
  def sum_all(*numbers):
      return sum(numbers)
  
  sum_all(1, 2, 3, 4)  # 10
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль функций — часть 1

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day12_practice.py`:

```python
# Задача 1: Факториал (функция)
# def factorial(n) → n!

# Задача 2: Проверка простоты (функция)
# def is_prime(n) → True/False

# Задача 3: Палиндром (функция)
# def is_palindrome(s) → True/False

# Задача 4: Статистика списка
# def stats(lst) → словарь с min, max, sum, avg

# Задача 5: Генератор паролей
# def generate_password(length=8, digits=True, special=True)
# → случайный пароль

# Задача 6: Валидация email
# def is_valid_email(email) → True/False
# Должен содержать @, точку после @, текст до и после @
```

### 13:45–14:00 | Итоги дня
- [ ] Правило: функция должна делать ОДНУ вещь
- [ ] Хорошие имена: глагол + существительное (get_user, calculate_sum)

---

## 📅 День 13 — Функции: продвинутые темы

### Блок 1 (9:00–10:00) | Теория: Область видимости
- [ ] Локальные и глобальные переменные:
  ```python
  x = 10  # глобальная
  
  def foo():
      x = 20  # локальная (новая!)
      print(x)  # 20
  
  foo()
  print(x)  # 10
  ```
- [ ] Ключевое слово `global` (избегать!):
  ```python
  def change_x():
      global x
      x = 20
  ```
- [ ] Правило LEGB: Local → Enclosing → Global → Built-in

### Блок 2 (10:15–11:15) | Теория: Lambda и функции высшего порядка
- [ ] Lambda-функции:
  ```python
  square = lambda x: x ** 2
  add = lambda a, b: a + b
  ```
- [ ] `map()` — применить функцию ко всем элементам:
  ```python
  nums = [1, 2, 3, 4]
  squares = list(map(lambda x: x**2, nums))
  ```
- [ ] `filter()` — отфильтровать элементы:
  ```python
  evens = list(filter(lambda x: x % 2 == 0, nums))
  ```
- [ ] `sorted()` с ключом:
  ```python
  words = ["banana", "pie", "apple"]
  sorted(words, key=len)  # ['pie', 'apple', 'banana']
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik + Codewars
- [ ] Stepik: модуль функций — часть 2
- [ ] Codewars: 5 задач на функции (7-6 kyu)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day13_practice.py`:

```python
# Задача 1: map — длины строк
# Список строк → список их длин (через map + lambda)

# Задача 2: filter — положительные
# Оставить только положительные числа (через filter)

# Задача 3: Сортировка словарей
# Список словарей с людьми — отсортируй по возрасту
# people = [{"name": "Аня", "age": 25}, {"name": "Боря", "age": 20}]

# Задача 4: Декоратор-таймер (простой)
# Функция, которая замеряет время выполнения другой функции
# import time

# Задача 5: Генератор отчёта
# def generate_report(data, formatter=str)
# Применяет formatter к каждому элементу
```

### 13:45–14:00 | Итоги дня
- [ ] Lambda — для коротких одноразовых функций
- [ ] map/filter vs list comprehension — знать оба способа

---

## 📅 День 14 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Коллекции
- [ ] Шпаргалки по спискам, кортежам, множествам, словарям
- [ ] Тест: создать каждую структуру, добавить/удалить элемент
- [ ] Когда что использовать?

### Блок 2 (10:15–11:15) | Повторение: Функции
- [ ] Написать по памяти: функция с *args, со значениями по умолчанию
- [ ] Примеры с map, filter, lambda
- [ ] Область видимости — local vs global

### Блок 3 (11:30–12:30) | Повторение: Comprehensions
- [ ] List comprehension: `[x**2 for x in range(10) if x % 2 == 0]`
- [ ] Dict comprehension: `{x: x**2 for x in range(5)}`
- [ ] Переписать 3 старые задачи с comprehension

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 2

## Задача: «Анализатор текста»

Напиши программу для анализа текста.

**Функционал:**

1. **Ввод текста** (многострочный, заканчивается пустой строкой)

2. **Базовая статистика:**
   - Количество символов (с пробелами и без)
   - Количество слов
   - Количество предложений (по `.!?`)
   - Средняя длина слова
   - Средняя длина предложения (в словах)

3. **Частотный анализ:**
   - Топ-10 самых частых слов (без учёта регистра)
   - Топ-5 самых длинных слов

4. **Поиск:**
   - Найти все слова, содержащие подстроку
   - Найти предложения с ключевым словом

**Требования к коду:**
- Минимум 5 функций (разбей логику!)
- Использовать словари для подсчёта частот
- Использовать list comprehension
- Меню для выбора действия

**Пример:**
```
=== Анализатор текста ===
Введите текст (пустая строка — конец):
Python это отличный язык. Python прост в изучении.
Python используют везде!

=== Статистика ===
Символов: 89 (без пробелов: 76)
Слов: 11
Предложений: 3

=== Топ-3 слов ===
python: 3
это: 1
отличный: 1

Что показать? (1-статистика, 2-частоты, 3-поиск, 0-выход):
```

---

# НЕДЕЛЯ 3: Файлы, модули, ООП

> **Цель недели:** Научиться работать с файлами, использовать модули, понять основы ООП

---

## 📅 День 15 — Работа с файлами

### Блок 1 (9:00–10:00) | Теория: Чтение и запись
- [ ] Открытие файла:
  ```python
  # Правильный способ (with)
  with open("file.txt", "r", encoding="utf-8") as f:
      content = f.read()
  # Файл закроется автоматически
  ```
- [ ] Режимы открытия:
  - `"r"` — чтение (по умолчанию)
  - `"w"` — запись (перезапись!)
  - `"a"` — добавление в конец
  - `"r+"` — чтение и запись

### Блок 2 (10:15–11:15) | Теория: Методы чтения и записи
- [ ] Чтение:
  ```python
  f.read()        # весь файл как строка
  f.readline()    # одна строка
  f.readlines()   # список строк
  
  # Построчная итерация (лучший способ для больших файлов)
  for line in f:
      print(line.strip())
  ```
- [ ] Запись:
  ```python
  f.write("текст")       # записать строку
  f.writelines(list)     # записать список строк
  ```

### Блок 3 (11:30–12:30) | Практика: эксперименты с файлами
- [ ] Создай файл `test.txt` вручную с несколькими строками
- [ ] Прочитай его разными способами
- [ ] Запиши новый файл
- [ ] Stepik: модуль работы с файлами

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day15_practice.py`:

```python
# Задача 1: Копирование файла
# Прочитай файл, запиши копию с именем file_copy.txt

# Задача 2: Нумерация строк
# Прочитай файл, запиши новый с номерами строк:
# 1: первая строка
# 2: вторая строка

# Задача 3: Поиск в файле
# Найди и выведи все строки, содержащие заданное слово

# Задача 4: Статистика файла
# Подсчитай: количество строк, слов, символов

# Задача 5: Объединение файлов
# Объедини несколько .txt файлов в один

# Задача 6: Простой логгер
# Функция log(message), которая добавляет строку в лог-файл
# Формат: "2025-01-16 10:30:00 - сообщение"
# Подсказка: from datetime import datetime
```

### 13:45–14:00 | Итоги дня
- [ ] Всегда используй `with open()`!
- [ ] Всегда указывай `encoding="utf-8"`

---

## 📅 День 16 — JSON и работа с данными

### Блок 1 (9:00–10:00) | Теория: JSON
- [ ] JSON — универсальный формат обмена данными:
  ```python
  import json
  
  data = {"name": "Иван", "age": 25, "skills": ["Python", "SQL"]}
  
  # Python → JSON (сериализация)
  json_string = json.dumps(data, ensure_ascii=False, indent=2)
  
  # JSON → Python (десериализация)
  data = json.loads(json_string)
  ```
- [ ] Работа с файлами:
  ```python
  # Запись в файл
  with open("data.json", "w", encoding="utf-8") as f:
      json.dump(data, f, ensure_ascii=False, indent=2)
  
  # Чтение из файла
  with open("data.json", "r", encoding="utf-8") as f:
      data = json.load(f)
  ```

### Блок 2 (10:15–11:15) | Теория: Хранение данных
- [ ] JSON как простая база данных:
  ```python
  # employees.json
  {
      "employees": [
          {"id": 1, "name": "Иван", "rate": 500},
          {"id": 2, "name": "Мария", "rate": 600}
      ]
  }
  ```
- [ ] CRUD операции:
  - Create — добавить запись
  - Read — прочитать
  - Update — обновить
  - Delete — удалить

### Блок 3 (11:30–12:30) | Практика: JSON
- [ ] Создай JSON-файл с данными о 5 сотрудниках
- [ ] Прочитай, измени ставку одному, сохрани
- [ ] Stepik: задачи на JSON (если есть)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day16_practice.py`:

```python
# Задача 1: База сотрудников (JSON)
# Структура: id, name, phone, hourly_rate
# Функции: add_employee, get_all, get_by_id, update_rate, delete

# Задача 2: Конфиг приложения
# Загрузи настройки из config.json
# Если файла нет — создай с настройками по умолчанию

# Задача 3: История операций
# Каждое действие (добавление, удаление) логируй в JSON
# {"timestamp": "...", "action": "add", "data": {...}}

# Задача 4: Поиск по базе
# Найди всех сотрудников со ставкой выше N

# Задача 5: Экспорт данных
# Преобразуй JSON в читаемый текстовый отчёт
```

### 13:45–14:00 | Итоги дня
- [ ] JSON — идеален для конфигов и небольших баз данных
- [ ] `json.dump` / `json.load` — для файлов
- [ ] `json.dumps` / `json.loads` — для строк

---

## 📅 День 17 — Модули и структура проекта

### Блок 1 (9:00–10:00) | Теория: Импорты
- [ ] Импорт модуля:
  ```python
  import math
  math.sqrt(16)  # 4.0
  
  from math import sqrt, pi
  sqrt(16)  # 4.0
  
  import math as m  # псевдоним
  m.sqrt(16)
  ```
- [ ] Полезные стандартные модули:
  - `datetime` — дата и время
  - `random` — случайные числа
  - `os` — работа с файловой системой
  - `json` — уже знаем!

### Блок 2 (10:15–11:15) | Теория: Создание своих модулей
- [ ] Свой модуль — просто `.py` файл:
  ```python
  # utils.py
  def format_date(date):
      return date.strftime("%Y%m%d")
  
  def calculate_payment(hours, rate):
      return hours * rate
  ```
  ```python
  # main.py
  from utils import format_date, calculate_payment
  
  payment = calculate_payment(8, 500)
  ```
- [ ] `if __name__ == "__main__":` — код только при прямом запуске
- [ ] Структура проекта:
  ```
  my_project/
  ├── main.py
  ├── database.py
  ├── utils.py
  └── data/
      └── employees.json
  ```

### Блок 3 (11:30–12:30) | Практика: datetime и os
- [ ] `datetime`:
  ```python
  from datetime import datetime, timedelta
  
  now = datetime.now()
  event_date = datetime(2025, 1, 20)
  formatted = event_date.strftime("%Y%m%d")  # "20250120"
  
  tomorrow = now + timedelta(days=1)
  ```
- [ ] `os`:
  ```python
  import os
  
  os.listdir(".")        # файлы в папке
  os.path.exists("file.txt")  # существует ли
  os.makedirs("folder", exist_ok=True)  # создать папку
  ```

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day17_practice.py`:

```python
# Задача 1: Модуль utils.py
# Создай модуль с функциями:
# - format_event_name(date, company, work_type) → "20250116-МузСервис-погрузка"
# - parse_event_name(name) → {"date": ..., "company": ..., "type": ...}
# - calculate_payment(hours, rate, bonus=0)

# Задача 2: Работа с датами
# Введи дату мероприятия, выведи:
# - день недели
# - сколько дней до мероприятия
# - дата в формате "16 января 2025"

# Задача 3: Менеджер файлов
# Функции: список файлов в папке, создание папки, проверка существования

# Задача 4: Структура проекта
# Создай папку event_manager/ с файлами:
# - main.py (точка входа)
# - database.py (работа с JSON)
# - utils.py (вспомогательные функции)
```

### 13:45–14:00 | Итоги дня
- [ ] Разбивай код на модули по смыслу
- [ ] datetime — твой друг для работы с датами

---

## 📅 День 18 — ООП: классы и объекты

### Блок 1 (9:00–10:00) | Теория: Основы ООП
- [ ] Класс — шаблон для объектов:
  ```python
  class Employee:
      def __init__(self, name, phone, hourly_rate):
          self.name = name
          self.phone = phone
          self.hourly_rate = hourly_rate
      
      def calculate_payment(self, hours):
          return hours * self.hourly_rate
  
  # Создание объекта
  ivan = Employee("Иван", "+79991234567", 500)
  print(ivan.name)  # Иван
  print(ivan.calculate_payment(8))  # 4000
  ```
- [ ] `self` — ссылка на текущий объект
- [ ] `__init__` — конструктор (инициализация)

### Блок 2 (10:15–11:15) | Теория: Методы и атрибуты
- [ ] Методы класса:
  ```python
  class Event:
      def __init__(self, date, company, work_type):
          self.date = date
          self.company = company
          self.work_type = work_type
          self.employees = []
      
      def add_employee(self, employee):
          self.employees.append(employee)
      
      def get_name(self):
          return f"{self.date}-{self.company}-{self.work_type}"
      
      def __str__(self):  # для print()
          return f"Event: {self.get_name()}"
  ```
- [ ] Специальные методы:
  - `__str__` — строковое представление
  - `__repr__` — для отладки

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль ООП — часть 1
- [ ] Создать классы Employee и Event

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day18_practice.py`:

```python
# Задача 1: Класс Employee
# Атрибуты: id, name, phone, hourly_rate
# Методы: 
# - calculate_payment(hours, bonus=0)
# - __str__ (красивый вывод)

# Задача 2: Класс Event
# Атрибуты: date, company, work_type, employees (список)
# Методы:
# - get_formatted_name() → "20250116-МузСервис-погрузка"
# - add_employee(employee)
# - remove_employee(employee_id)
# - get_total_cost(hours_dict) — словарь {employee_id: hours}

# Задача 3: Класс WorkLog
# Запись о работе: employee_id, event_id, hours, date
# Методы: calculate_payment(rate)

# Задача 4: Взаимодействие классов
# Создай 3 сотрудников, 1 мероприятие
# Добавь сотрудников на мероприятие
# Рассчитай общую стоимость
```

### 13:45–14:00 | Итоги дня
- [ ] Класс = данные + поведение
- [ ] Объект = конкретный экземпляр класса

---

## 📅 День 19 — ООП: продолжение

### Блок 1 (9:00–10:00) | Теория: Наследование
- [ ] Расширение классов:
  ```python
  class Person:
      def __init__(self, name, phone):
          self.name = name
          self.phone = phone
  
  class Employee(Person):
      def __init__(self, name, phone, hourly_rate):
          super().__init__(name, phone)  # вызов родителя
          self.hourly_rate = hourly_rate
  
  class Manager(Person):
      def __init__(self, name, phone, department):
          super().__init__(name, phone)
          self.department = department
  ```

### Блок 2 (10:15–11:15) | Теория: Инкапсуляция и свойства
- [ ] Приватные атрибуты (соглашение):
  ```python
  class Employee:
      def __init__(self, name, rate):
          self.name = name
          self._rate = rate  # "защищённый"
      
      @property
      def rate(self):
          return self._rate
      
      @rate.setter
      def rate(self, value):
          if value > 0:
              self._rate = value
          else:
              raise ValueError("Ставка должна быть положительной")
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik + эксперименты
- [ ] Stepik: ООП — часть 2
- [ ] Добавить валидацию в свои классы

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day19_practice.py`:

```python
# Задача 1: Иерархия классов
# Person → Employee, Client, Manager
# У каждого свои атрибуты и методы

# Задача 2: Валидация данных
# В классе Employee:
# - rate не может быть отрицательным
# - phone должен начинаться с +7

# Задача 3: Класс Database
# Хранит список объектов (employees, events)
# Методы: add, get_by_id, get_all, delete, save_to_json, load_from_json

# Задача 4: Сериализация объектов
# Метод to_dict() — превращает объект в словарь для JSON
# Метод from_dict(data) — создаёт объект из словаря
```

### 13:45–14:00 | Итоги дня
- [ ] Наследование — для расширения функциональности
- [ ] @property — контроль доступа к атрибутам

---

## 📅 День 20 — Обработка ошибок

### Блок 1 (9:00–10:00) | Теория: try-except
- [ ] Базовая обработка:
  ```python
  try:
      rate = int(input("Ставка: "))
      result = 1000 / rate
  except ValueError:
      print("Введите число!")
  except ZeroDivisionError:
      print("Ставка не может быть нулём!")
  ```
- [ ] Общий перехват:
  ```python
  except Exception as e:
      print(f"Ошибка: {e}")
  ```

### Блок 2 (10:15–11:15) | Теория: else, finally, raise
- [ ] Полная конструкция:
  ```python
  try:
      data = load_from_json("data.json")
  except FileNotFoundError:
      print("Файл не найден, создаю новый...")
      data = {}
  else:
      print("Данные загружены успешно")
  finally:
      print("Операция завершена")  # выполнится всегда
  ```
- [ ] Вызов исключения:
  ```python
  def set_rate(rate):
      if rate <= 0:
          raise ValueError("Ставка должна быть положительной")
      return rate
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль обработки исключений

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day20_practice.py`:

```python
# Задача 1: Безопасный ввод
# Функция safe_input_int(prompt) — запрашивает число, пока не введут число

# Задача 2: Безопасная загрузка JSON
# Функция load_data(path) — если файл не найден или битый, возвращает {}

# Задача 3: Валидация с исключениями
# Класс Employee выбрасывает:
# - InvalidRateError если rate <= 0
# - InvalidPhoneError если телефон некорректный

# Задача 4: Транзакция
# Функция transfer_employee(from_event, to_event, employee_id)
# Если что-то пошло не так — откатить изменения

# Задача 5: Логирование ошибок
# При ошибке записывай в errors.log: дата, тип ошибки, сообщение
```

### 13:45–14:00 | Итоги дня
- [ ] try-except — для ожидаемых ошибок
- [ ] raise — когда нужно сообщить об ошибке вызывающему коду

---

## 📅 День 21 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Файлы и JSON
- [ ] Шпаргалки: работа с файлами, JSON
- [ ] Тест: прочитать JSON, изменить, записать

### Блок 2 (10:15–11:15) | Повторение: ООП
- [ ] Написать по памяти: класс с __init__, методами, __str__
- [ ] Примеры: наследование, @property

### Блок 3 (11:30–12:30) | Повторение: Исключения
- [ ] Полная конструкция try-except-else-finally
- [ ] Свои исключения

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 3

## Задача: «Система учёта сотрудников»

Консольное приложение для управления сотрудниками и мероприятиями.

**Классы:**
```python
Employee:
    - id, name, phone, hourly_rate
    - to_dict(), from_dict()
    
Event:
    - id, date, company, work_type
    - employees: list[int]  # id сотрудников
    - get_formatted_name()
    - to_dict(), from_dict()

Database:
    - employees: list[Employee]
    - events: list[Event]
    - add_employee(), get_employee(), delete_employee()
    - add_event(), get_event(), assign_employee_to_event()
    - save(), load()
```

**Функционал:**
1. CRUD для сотрудников
2. CRUD для мероприятий
3. Назначение сотрудников на мероприятие
4. Расчёт оплаты для мероприятия (ввод часов)
5. Сохранение/загрузка из JSON

**Интерфейс:**
```
=== Менеджер мероприятий ===
1. Сотрудники
2. Мероприятия
3. Назначения
4. Расчёт оплаты
0. Выход

> 1

=== Сотрудники ===
1. Список всех
2. Добавить
3. Редактировать
4. Удалить
0. Назад
```

---

# НЕДЕЛЯ 4: API, веб-интерфейс, Telegram

> **Цель недели:** Сделать веб-интерфейс и интеграцию с Telegram

---

## 📅 День 22 — Основы веб: Streamlit

### Блок 1 (9:00–10:00) | Теория: Введение в Streamlit
- [ ] Установка: `pip install streamlit`
- [ ] Запуск: `streamlit run app.py`
- [ ] Базовые элементы:
  ```python
  import streamlit as st
  
  st.title("Менеджер мероприятий")
  st.header("Сотрудники")
  st.write("Текст или данные")
  
  # Ввод
  name = st.text_input("Имя")
  rate = st.number_input("Ставка", min_value=0)
  
  if st.button("Добавить"):
      st.success(f"Добавлен: {name}")
  ```

### Блок 2 (10:15–11:15) | Теория: Элементы интерфейса
- [ ] Выбор и загрузка:
  ```python
  # Выпадающий список
  company = st.selectbox("Компания", ["МузСервис", "EventPro", "Другое"])
  
  # Множественный выбор
  employees = st.multiselect("Сотрудники", ["Иван", "Мария", "Пётр"])
  
  # Дата
  event_date = st.date_input("Дата мероприятия")
  
  # Загрузка файла
  uploaded = st.file_uploader("Загрузить шаблон")
  ```
- [ ] Отображение данных:
  ```python
  # Таблица
  st.dataframe(data)  # интерактивная
  st.table(data)      # статичная
  
  # Метрики
  st.metric("Всего сотрудников", 15, "+2")
  ```

### Блок 3 (11:30–12:30) | Практика: Первое приложение
- [ ] Создать app.py с формой добавления сотрудника
- [ ] Отображение списка в таблице
- [ ] Кнопки редактирования/удаления

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day22_practice.py` → `day22_app.py`:

```python
# Задача 1: Калькулятор оплаты
# Ввод: часы, ставка, бонус
# Вывод: итоговая сумма

# Задача 2: Форма сотрудника
# Поля: имя, телефон, ставка
# Кнопка "Добавить" → показать данные

# Задача 3: Таблица мероприятий
# Отобрази список мероприятий из JSON
# Добавь фильтр по компании

# Задача 4: Метрики
# Покажи: всего сотрудников, средняя ставка, мероприятий в этом месяце
```

### 13:45–14:00 | Итоги дня
- [ ] Streamlit — самый быстрый способ сделать веб-интерфейс
- [ ] Каждое изменение = перезапуск скрипта (это нормально)

---

## 📅 День 23 — Streamlit: состояние и страницы

### Блок 1 (9:00–10:00) | Теория: Session State
- [ ] Проблема: Streamlit перезапускается при каждом действии
- [ ] Решение — session_state:
  ```python
  # Инициализация
  if "employees" not in st.session_state:
      st.session_state.employees = []
  
  # Использование
  if st.button("Добавить"):
      st.session_state.employees.append(new_employee)
  
  # Отображение
  st.write(st.session_state.employees)
  ```

### Блок 2 (10:15–11:15) | Теория: Многостраничность
- [ ] Структура:
  ```
  app/
  ├── app.py           # главная
  └── pages/
      ├── 1_Сотрудники.py
      ├── 2_Мероприятия.py
      └── 3_Отчёты.py
  ```
- [ ] Sidebar для навигации:
  ```python
  st.sidebar.title("Меню")
  page = st.sidebar.radio("", ["Главная", "Сотрудники", "Мероприятия"])
  ```

### Блок 3 (11:30–12:30) | Практика: Многостраничное приложение
- [ ] Создать структуру с 3 страницами
- [ ] Общие данные через session_state
- [ ] Навигация через sidebar

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
```python
# Задача 1: CRUD сотрудников на одной странице
# - Таблица с текущими
# - Форма добавления
# - Кнопки "Редактировать" / "Удалить"

# Задача 2: Страница мероприятий
# - Форма: дата, компания, тип работы
# - Автогенерация названия
# - Список мероприятий

# Задача 3: Назначение на мероприятие
# - Выбор мероприятия (selectbox)
# - Мультивыбор сотрудников
# - Отображение назначенных

# Задача 4: Сохранение данных
# - Кнопка "Сохранить" → JSON
# - Автозагрузка при старте
```

### 13:45–14:00 | Итоги дня
- [ ] session_state — для сохранения данных между перезапусками
- [ ] Папка pages/ — автоматическая многостраничность

---

## 📅 День 24 — Работа с Excel (openpyxl)

### Блок 1 (9:00–10:00) | Теория: Чтение Excel
- [ ] Установка: `pip install openpyxl`
- [ ] Чтение файла:
  ```python
  from openpyxl import load_workbook
  
  wb = load_workbook("template.xlsx")
  sheet = wb.active  # или wb["Sheet1"]
  
  # Чтение ячейки
  value = sheet["A1"].value
  value = sheet.cell(row=1, column=1).value
  
  # Чтение всех строк
  for row in sheet.iter_rows(min_row=2, values_only=True):
      print(row)  # кортеж значений
  ```

### Блок 2 (10:15–11:15) | Теория: Запись в Excel
- [ ] Создание и запись:
  ```python
  from openpyxl import Workbook
  
  wb = Workbook()
  sheet = wb.active
  
  # Запись в ячейку
  sheet["A1"] = "Имя"
  sheet["B1"] = "Ставка"
  sheet.cell(row=2, column=1, value="Иван")
  
  # Добавление строки
  sheet.append(["Мария", 600])
  
  wb.save("output.xlsx")
  ```
- [ ] Работа с шаблоном:
  ```python
  # Открыть шаблон, заполнить, сохранить как новый
  wb = load_workbook("template.xlsx")
  sheet = wb.active
  sheet["B5"] = "Иванов Иван"
  sheet["B6"] = 5000
  wb.save("договор_Иванов.xlsx")
  ```

### Блок 3 (11:30–12:30) | Практика: Работа с шаблонами
- [ ] Создай шаблон договора в Excel с местами для заполнения
- [ ] Напиши скрипт, который заполняет шаблон данными

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day24_practice.py`:

```python
# Задача 1: Чтение базы сотрудников из Excel
# Столбцы: Имя, Телефон, Ставка
# Преобразуй в список словарей

# Задача 2: Генератор договора
# Шаблон с полями: ФИО, дата, сумма
# Функция generate_contract(employee, amount, date) → файл

# Задача 3: Генератор акта
# Шаблон акта выполненных работ
# Поля: мероприятие, сотрудник, часы, сумма

# Задача 4: Табель учёта
# Создай Excel с табелем:
# Столбцы: Сотрудник | 1 | 2 | 3 | ... | 31 | Итого часов | Сумма

# Задача 5: Интеграция со Streamlit
# Кнопка "Скачать договор" → генерация и download
```

### 13:45–14:00 | Итоги дня
- [ ] openpyxl — полный контроль над Excel
- [ ] Шаблоны — создавай в Excel, заполняй кодом

---

## 📅 День 25 — Telegram API

### Блок 1 (9:00–10:00) | Теория: Telegram API
- [ ] Два способа работы с Telegram:
  - **Bot API** — для ботов (проще)
  - **MTProto API** — для пользовательских действий (создание чатов!)
- [ ] Для создания чатов нужен MTProto (Telethon/Pyrogram)
- [ ] Установка: `pip install telethon`
- [ ] Получение API credentials:
  1. Зайти на my.telegram.org
  2. Создать приложение
  3. Получить api_id и api_hash

### Блок 2 (10:15–11:15) | Теория: Telethon
- [ ] Базовый клиент:
  ```python
  from telethon import TelegramClient
  
  api_id = "YOUR_API_ID"
  api_hash = "YOUR_API_HASH"
  
  client = TelegramClient("session_name", api_id, api_hash)
  
  async def main():
      await client.start()
      me = await client.get_me()
      print(f"Logged in as {me.first_name}")
  
  client.loop.run_until_complete(main())
  ```
- [ ] Создание группы:
  ```python
  from telethon.tl.functions.messages import CreateChatRequest
  
  async def create_group(name, user_ids):
      result = await client(CreateChatRequest(
          users=user_ids,
          title=name
      ))
      return result
  ```

### Блок 3 (11:30–12:30) | Практика: Настройка Telegram
- [ ] Зарегистрировать приложение на my.telegram.org
- [ ] Создать тестовый скрипт подключения
- [ ] Попробовать отправить сообщение себе

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day25_practice.py`:

```python
# Задача 1: Подключение к Telegram
# Скрипт, который подключается и выводит твоё имя

# Задача 2: Получение контактов
# Получи список контактов/диалогов

# Задача 3: Создание группы
# Функция create_event_chat(date, company, work_type)
# Создаёт группу с названием "20250116-МузСервис-погрузка"

# Задача 4: Добавление участников
# Функция add_users_to_chat(chat, usernames)
# Добавляет пользователей по username

# Задача 5: Отправка сообщения
# После создания чата отправить приветственное сообщение
```

### 13:45–14:00 | Итоги дня
- [ ] Telethon — для пользовательских действий
- [ ] Храни api_id и api_hash в .env!

---

## 📅 День 26 — Интеграция Telegram + Streamlit

### Блок 1 (9:00–10:00) | Теория: Асинхронность
- [ ] Telegram API асинхронный, Streamlit — нет
- [ ] Решение:
  ```python
  import asyncio
  from telethon import TelegramClient
  
  # Создаём функцию-обёртку
  def create_chat_sync(name, users):
      loop = asyncio.new_event_loop()
      asyncio.set_event_loop(loop)
      return loop.run_until_complete(create_chat_async(name, users))
  
  async def create_chat_async(name, users):
      async with TelegramClient("session", api_id, api_hash) as client:
          # создание чата
          pass
  ```

### Блок 2 (10:15–11:15) | Теория: Модуль telegram_utils
- [ ] Вынести всю логику Telegram в отдельный модуль:
  ```python
  # telegram_utils.py
  
  class TelegramManager:
      def __init__(self, api_id, api_hash, session_name):
          self.client = TelegramClient(session_name, api_id, api_hash)
      
      def create_event_chat(self, event):
          """Создаёт чат для мероприятия"""
          name = event.get_formatted_name()
          # ...
      
      def add_participants(self, chat_id, usernames):
          """Добавляет участников в чат"""
          # ...
  ```

### Блок 3 (11:30–12:30) | Практика: Интеграция
- [ ] Добавить в Streamlit кнопку "Создать чат в Telegram"
- [ ] При нажатии — создаётся группа с названием мероприятия

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
```python
# Задача 1: Модуль TelegramManager
# Класс с методами:
# - connect()
# - create_chat(name)
# - add_users(chat_id, usernames)
# - send_message(chat_id, text)

# Задача 2: Интеграция в интерфейс
# Страница мероприятия:
# - Кнопка "Создать Telegram-чат"
# - Ввод username'ов сотрудников
# - Статус создания чата

# Задача 3: Хранение chat_id
# После создания чата сохраняй его ID в данных мероприятия
# Чтобы не создавать повторно

# Задача 4: Приветственное сообщение
# После создания чата отправь сообщение с деталями мероприятия:
# "Мероприятие: ..."
# "Дата: ..."
# "Компания: ..."
```

### 13:45–14:00 | Итоги дня
- [ ] Асинхронный код оборачиваем для Streamlit
- [ ] Вся логика Telegram — в отдельном модуле

---

## 📅 День 27 — Базы данных: SQLite

### Блок 1 (9:00–10:00) | Теория: Почему база данных
- [ ] JSON vs SQLite:
  - JSON: просто, но медленно на больших данных
  - SQLite: быстрее, надёжнее, SQL-запросы
- [ ] SQLite — файловая БД, не нужен сервер
- [ ] Базовые операции:
  ```python
  import sqlite3
  
  # Подключение (создаст файл если нет)
  conn = sqlite3.connect("database.db")
  cursor = conn.cursor()
  
  # Создание таблицы
  cursor.execute("""
      CREATE TABLE IF NOT EXISTS employees (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          name TEXT NOT NULL,
          phone TEXT,
          hourly_rate REAL
      )
  """)
  conn.commit()
  ```

### Блок 2 (10:15–11:15) | Теория: CRUD с SQLite
- [ ] Вставка:
  ```python
  cursor.execute(
      "INSERT INTO employees (name, phone, hourly_rate) VALUES (?, ?, ?)",
      ("Иван", "+79991234567", 500)
  )
  conn.commit()
  ```
- [ ] Чтение:
  ```python
  cursor.execute("SELECT * FROM employees")
  rows = cursor.fetchall()  # список кортежей
  
  cursor.execute("SELECT * FROM employees WHERE id = ?", (1,))
  row = cursor.fetchone()
  ```
- [ ] Обновление и удаление:
  ```python
  cursor.execute("UPDATE employees SET hourly_rate = ? WHERE id = ?", (600, 1))
  cursor.execute("DELETE FROM employees WHERE id = ?", (1,))
  conn.commit()
  ```

### Блок 3 (11:30–12:30) | Практика: Перевод на SQLite
- [ ] Создать схему БД для проекта
- [ ] Написать класс Database с методами CRUD

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day27_practice.py`:

```python
# Задача 1: Схема базы данных
# Таблицы:
# - employees (id, name, phone, hourly_rate, telegram_username)
# - events (id, date, company, work_type, telegram_chat_id)
# - assignments (id, employee_id, event_id, hours, bonus)

# Задача 2: Класс DatabaseManager
# Методы для каждой таблицы:
# - add_employee(), get_employee(), get_all_employees(), update_employee(), delete_employee()
# - То же для events
# - assign_employee_to_event(), get_event_employees()

# Задача 3: Миграция с JSON
# Скрипт, который читает данные из JSON и переносит в SQLite

# Задача 4: Отчёты через SQL
# - Сумма оплаты по сотруднику за месяц
# - Список мероприятий с количеством сотрудников
# - Топ-5 сотрудников по часам
```

### 13:45–14:00 | Итоги дня
- [ ] SQLite — надёжнее JSON для реальных приложений
- [ ] Используй параметризованные запросы (?) для безопасности

---

## 📅 День 28 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Streamlit
- [ ] Основные элементы: input, button, selectbox, dataframe
- [ ] session_state для хранения данных
- [ ] Многостраничность

### Блок 2 (10:15–11:15) | Повторение: Excel и Telegram
- [ ] openpyxl: чтение, запись, шаблоны
- [ ] Telethon: подключение, создание чатов

### Блок 3 (11:30–12:30) | Повторение: SQLite
- [ ] CRUD операции
- [ ] Связи между таблицами

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 4

## Задача: «Прототип менеджера мероприятий»

Объедини всё изученное в рабочий прототип.

**Требования:**

1. **Веб-интерфейс (Streamlit):**
   - Страница сотрудников (CRUD)
   - Страница мероприятий (CRUD)
   - Страница назначений

2. **База данных (SQLite):**
   - Таблицы: employees, events, assignments
   - Все данные сохраняются

3. **Генерация документов:**
   - Кнопка "Скачать договор" → Excel из шаблона
   - Кнопка "Скачать акт" → Excel из шаблона

4. **Telegram (если настроен):**
   - Кнопка "Создать чат" на странице мероприятия

**Структура проекта:**
```
event_manager/
├── app.py
├── pages/
│   ├── 1_👥_Сотрудники.py
│   ├── 2_📅_Мероприятия.py
│   └── 3_📊_Отчёты.py
├── database.py
├── telegram_utils.py
├── document_generator.py
├── templates/
│   ├── contract_template.xlsx
│   └── act_template.xlsx
└── data/
    └── database.db
```

---

# ФИНАЛ: Дни 29-31

---

## 📅 День 29 — Подготовка финального проекта

### Блок 1-2 (9:00–11:15) | Анализ и планирование
- [ ] Проверить, что работает из прототипа дня 28
- [ ] Список багов и недоделок
- [ ] Приоритеты: что обязательно, что можно отложить
- [ ] Финальная структура проекта

### Блок 3-4 (11:30–13:45) | Подготовка
- [ ] Создать финальную структуру папок
- [ ] Подготовить шаблоны документов (договор, акт)
- [ ] Настроить .env с ключами
- [ ] Начать писать README.md

---

## 📅 День 30 — Разработка

### 9:00–14:00 | Полный день кодинга
- [ ] Допилить все страницы интерфейса
- [ ] Отладить работу с базой данных
- [ ] Проверить генерацию документов
- [ ] Telegram-интеграция (если есть время)
- [ ] Тестирование всех сценариев

---

## 📅 День 31 — Финализация и демо

### Блок 1-2 (9:00–11:15) | Доработка
- [ ] Исправить баги
- [ ] Улучшить UX (сообщения об успехе/ошибках)
- [ ] Добавить валидацию данных
- [ ] Причесать код (удалить print, добавить комментарии)

### Блок 3 (11:30–12:30) | Документация
- [ ] Дописать README.md:
  - Описание проекта
  - Установка и запуск
  - Скриншоты
  - Что реализовано / что планируется

### Блок 4 (12:45–13:45) | Демонстрация
- [ ] Финальное тестирование
- [ ] Записать скриншоты или короткое видео
- [ ] Подготовить код для отправки на проверку

---

# 🎯 ФИНАЛЬНЫЙ ПРОЕКТ

## «Менеджер мероприятий»

### Что должно работать:

**1. Управление сотрудниками:**
- Добавление/редактирование/удаление
- Поля: имя, телефон, ставка, Telegram username
- Поиск и фильтрация

**2. Управление мероприятиями:**
- Создание с автоформатированием названия (20250116-Компания-ТипРабот)
- Назначение сотрудников
- Указание часов работы

**3. Расчёт оплаты:**
- Автоматический подсчёт по часам и ставке
- Учёт бонусов
- Итоговая сумма по мероприятию

**4. Документы:**
- Генерация договора из шаблона Excel
- Генерация акта из шаблона Excel
- Скачивание через интерфейс

**5. Telegram (бонус):**
- Создание чата для мероприятия
- Добавление участников

### Технологии:
- Python
- Streamlit (веб-интерфейс)
- SQLite (база данных)
- openpyxl (Excel)
- Telethon (Telegram, опционально)

### Критерии оценки:
- ✅ Приложение запускается без ошибок
- ✅ CRUD для сотрудников работает
- ✅ CRUD для мероприятий работает
- ✅ Назначение сотрудников работает
- ✅ Расчёт оплаты корректный
- ✅ Генерация хотя бы одного документа
- ✅ Данные сохраняются между перезапусками
- ✅ Код структурирован по модулям
- ✅ Есть README

---

# 📝 Чек-лист прогресса

## Неделя 1: Основы
- [ ] День 1: Переменные, типы
- [ ] День 2: Операторы
- [ ] День 3: Условия
- [ ] День 4: while
- [ ] День 5: for
- [ ] День 6: Строки
- [ ] День 7: ✅ Задача «Валидатор паролей»

## Неделя 2: Структуры данных
- [ ] День 8: Списки (основы)
- [ ] День 9: Списки (comprehension)
- [ ] День 10: Кортежи, множества
- [ ] День 11: Словари
- [ ] День 12: Функции (основы)
- [ ] День 13: Функции (продвинутые)
- [ ] День 14: ✅ Задача «Анализатор текста»

## Неделя 3: Файлы, ООП
- [ ] День 15: Файлы
- [ ] День 16: JSON
- [ ] День 17: Модули
- [ ] День 18: ООП (классы)
- [ ] День 19: ООП (наследование)
- [ ] День 20: Исключения
- [ ] День 21: ✅ Задача «Система учёта»

## Неделя 4: Веб и интеграции
- [ ] День 22: Streamlit (основы)
- [ ] День 23: Streamlit (состояние)
- [ ] День 24: Excel (openpyxl)
- [ ] День 25: Telegram API
- [ ] День 26: Интеграция
- [ ] День 27: SQLite
- [ ] День 28: ✅ Задача «Прототип»

## Финал
- [ ] День 29: Планирование
- [ ] День 30: Разработка
- [ ] День 31: ✅ Финальный проект

---

**Удачи! 🚀**

После 31 дня скинь мне проект — проверю и дам обратную связь. 5 задач на функции (7-6 kyu)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day13_practice.py`:

```python
# Задача 1: map
# Список строк → список их длин (через map)

# Задача 2: filter
# Оставить только положительные числа (через filter)

# Задача 3: Сортировка словарей
# Список словарей с людьми — отсортируй по возрасту

# Задача 4: Композиция функций
# Напиши compose(f, g) — возвращает функцию f(g(x))

# Задача 5: Счётчик вызовов
# Функция, которая считает, сколько раз её вызвали
# Подсказка: используй атрибут функции func.count = 0

# Задача 6: Частичное применение
# def multiply_by(n):
#     return lambda x: x * n
# double = multiply_by(2)
# double(5)  # 10
```

### 13:45–14:00 | Итоги дня
- [ ] Lambda — короткие одноразовые функции
- [ ] map/filter — альтернатива циклам (но list comprehension часто удобнее)

---

## 📅 День 14 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Коллекции
- [ ] Шпаргалки по спискам, кортежам, множествам, словарям
- [ ] Тест: создать каждую структуру, добавить/удалить элемент
- [ ] Когда что использовать?

### Блок 2 (10:15–11:15) | Повторение: Функции
- [ ] Написать по памяти: функция с *args, с значениями по умолчанию
- [ ] Примеры с map, filter, lambda
- [ ] Область видимости: локальные vs глобальные

### Блок 3 (11:30–12:30) | Повторение: Comprehensions
- [ ] List comprehension: `[x**2 for x in range(10) if x % 2 == 0]`
- [ ] Dict comprehension: `{x: x**2 for x in range(5)}`
- [ ] Переписать 3 старые задачи с comprehension

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 2

## Задача: «База сотрудников (консольная)»

Это подготовка к твоему финальному проекту! Напиши программу управления сотрудниками.

**Структура сотрудника:**
```python
{
    "id": 1,
    "name": "Иванов Иван",
    "phone": "+79991234567",
    "hourly_rate": 300,  # ставка в час
    "skills": ["погрузка", "монтаж", "звук"]
}
```

**Функционал:**

1. **CRUD операции:**
   - Добавить сотрудника
   - Показать всех / по ID / поиск по имени
   - Редактировать данные
   - Удалить сотрудника

2. **Фильтрация:**
   - По навыку (показать всех, кто умеет "погрузка")
   - По ставке (дешевле N рублей/час)

3. **Расчёт зарплаты:**
   - Ввести ID сотрудника и количество часов
   - Вывести сумму к выплате

4. **Статистика:**
   - Всего сотрудников
   - Средняя ставка
   - Самые популярные навыки

**Требования к коду:**
- Данные хранить в списке словарей
- Минимум 5 функций (add_employee, find_employee, calculate_salary...)
- Меню с выбором действия (цикл while)
- Валидация ввода (телефон, ставка > 0)

**Пример интерфейса:**
```
=== База сотрудников ===
1. Добавить сотрудника
2. Все сотрудники
3. Найти по имени
4. Найти по навыку
5. Рассчитать зарплату
6. Статистика
0. Выход

Выбор: 5
ID сотрудника: 1
Часов отработано: 8

Иванов Иван: 8 ч × 300 ₽ = 2400 ₽
```

---

# НЕДЕЛЯ 3: Файлы, модули, ООП

> **Цель недели:** Научиться сохранять данные, структурировать код, работать с классами

---

## 📅 День 15 — Работа с файлами

### Блок 1 (9:00–10:00) | Теория: Чтение и запись
- [ ] Открытие файла:
  ```python
  # Правильный способ (with)
  with open("file.txt", "r", encoding="utf-8") as f:
      content = f.read()
  # Файл закроется автоматически
  ```
- [ ] Режимы открытия:
  - `"r"` — чтение (по умолчанию)
  - `"w"` — запись (перезапись!)
  - `"a"` — добавление в конец
  - `"r+"` — чтение и запись

### Блок 2 (10:15–11:15) | Теория: Методы чтения и записи
- [ ] Чтение:
  ```python
  f.read()        # весь файл как строка
  f.readline()    # одна строка
  f.readlines()   # список строк
  
  # Построчная итерация (лучший способ для больших файлов)
  for line in f:
      print(line.strip())
  ```
- [ ] Запись:
  ```python
  f.write("текст")       # записать строку
  f.writelines(lst)      # записать список строк
  print("текст", file=f) # print в файл
  ```

### Блок 3 (11:30–12:30) | Практика: работа с файлами
- [ ] Создай текстовый файл, прочитай разными способами
- [ ] Stepik: модуль работы с файлами

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day15_practice.py`:

```python
# Задача 1: Копирование файла
# Прочитай файл, запиши копию

# Задача 2: Нумерация строк
# Прочитай файл, запиши новый с номерами строк

# Задача 3: Поиск в файле
# Найди все строки, содержащие слово

# Задача 4: Статистика файла
# Количество строк, слов, символов

# Задача 5: Лог-файл
# Функция log(message) — добавляет строку с датой/временем
# from datetime import datetime
```

### 13:45–14:00 | Итоги дня
- [ ] Всегда используй `with open()`!
- [ ] Всегда указывай `encoding="utf-8"`

---

## 📅 День 16 — JSON

### Блок 1 (9:00–10:00) | Теория: Основы JSON
- [ ] JSON — универсальный формат данных:
  ```python
  import json
  
  data = {
      "name": "Иван",
      "age": 25,
      "skills": ["погрузка", "монтаж"]
  }
  
  # Python → JSON строка
  json_string = json.dumps(data, ensure_ascii=False, indent=2)
  
  # JSON строка → Python
  data = json.loads(json_string)
  ```

### Блок 2 (10:15–11:15) | Теория: JSON и файлы
- [ ] Работа с файлами:
  ```python
  # Сохранить в файл
  with open("data.json", "w", encoding="utf-8") as f:
      json.dump(data, f, ensure_ascii=False, indent=2)
  
  # Загрузить из файла
  with open("data.json", "r", encoding="utf-8") as f:
      data = json.load(f)
  ```
- [ ] Типичный паттерн — загрузка с дефолтом:
  ```python
  import os
  
  def load_data(filename):
      if os.path.exists(filename):
          with open(filename, "r", encoding="utf-8") as f:
              return json.load(f)
      return []  # пустой список, если файла нет
  ```

### Блок 3 (11:30–12:30) | Практика: JSON на практике
- [ ] Создай JSON с данными о 5 сотрудниках
- [ ] Прочитай, добавь нового, сохрани
- [ ] Stepik: задачи на JSON (если есть)

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day16_practice.py`:

```python
# Задача 1: База сотрудников с сохранением
# Переделай задачу дня 14 — сохраняй данные в JSON

# Задача 2: Конфиг приложения
# Загрузи настройки из config.json
# Если файла нет — создай с дефолтами

# Задача 3: История операций
# Каждое действие записывай в history.json с датой

# Задача 4: Экспорт/импорт
# Две функции: export_to_json(data, filename), import_from_json(filename)

# Задача 5: Слияние JSON-файлов
# Объедини данные из нескольких файлов в один
```

### 13:45–14:00 | Итоги дня
- [ ] JSON — основной формат для хранения данных
- [ ] `ensure_ascii=False` для русского текста!

---

## 📅 День 17 — Модули и datetime

### Блок 1 (9:00–10:00) | Теория: Импорты и свои модули
- [ ] Способы импорта:
  ```python
  import math
  from math import sqrt, pi
  from math import *  # не рекомендуется
  import math as m
  ```
- [ ] Свой модуль — просто `.py` файл:
  ```python
  # utils.py
  def calculate_salary(hours, rate):
      return hours * rate
  
  # main.py
  from utils import calculate_salary
  ```
- [ ] `if __name__ == "__main__":` — код только при прямом запуске

### Блок 2 (10:15–11:15) | Теория: datetime — работа с датами
- [ ] Основы datetime:
  ```python
  from datetime import datetime, date, timedelta
  
  now = datetime.now()
  today = date.today()
  
  # Создание даты
  event_date = date(2025, 2, 15)
  event_datetime = datetime(2025, 2, 15, 10, 30)
  
  # Форматирование
  now.strftime("%Y%m%d")        # "20250116"
  now.strftime("%d.%m.%Y %H:%M") # "16.01.2025 14:30"
  
  # Парсинг строки в дату
  dt = datetime.strptime("20250116", "%Y%m%d")
  ```
- [ ] Арифметика с датами:
  ```python
  tomorrow = today + timedelta(days=1)
  week_ago = today - timedelta(weeks=1)
  
  # Разница между датами
  delta = date(2025, 2, 15) - today
  print(delta.days)  # количество дней
  ```

### Блок 3 (11:30–12:30) | Практика: datetime
- [ ] Stepik: задачи на datetime
- [ ] Поэкспериментировать с форматами дат

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day17_practice.py`:

```python
# Задача 1: Генератор названия чата
# Формат: "20250116-МузСервис-погрузка"
# Функция: generate_chat_name(company, work_type) → строка

# Задача 2: Дней до мероприятия
# Введи дату мероприятия, выведи сколько дней осталось

# Задача 3: Возраст в днях
# Введи дату рождения, посчитай возраст в днях

# Задача 4: Фильтр по дате
# Список мероприятий с датами — покажи только будущие

# Задача 5: Календарь на неделю
# Выведи даты на 7 дней вперёд в формате "Пн, 16 января"
```

### 13:45–14:00 | Итоги дня
- [ ] Шпаргалка: коды форматирования (%Y, %m, %d, %H, %M)
- [ ] datetime — обязательно для работы с мероприятиями!

---

## 📅 День 18 — ООП: классы и объекты

### Блок 1 (9:00–10:00) | Теория: Основы ООП
- [ ] Класс — шаблон для объектов:
  ```python
  class Employee:
      def __init__(self, name, phone, hourly_rate):
          self.name = name
          self.phone = phone
          self.hourly_rate = hourly_rate
      
      def calculate_salary(self, hours):
          return hours * self.hourly_rate
      
      def __str__(self):
          return f"{self.name} ({self.hourly_rate} ₽/ч)"
  
  # Создание объекта
  emp = Employee("Иван Петров", "+79991234567", 300)
  print(emp.calculate_salary(8))  # 2400
  ```
- [ ] `self` — ссылка на текущий объект
- [ ] `__init__` — конструктор (инициализация)
- [ ] `__str__` — строковое представление

### Блок 2 (10:15–11:15) | Теория: Методы и атрибуты
- [ ] Класс Event (мероприятие):
  ```python
  class Event:
      def __init__(self, date, company, work_type):
          self.date = date
          self.company = company
          self.work_type = work_type
          self.employees = []  # список сотрудников
      
      def add_employee(self, employee):
          self.employees.append(employee)
      
      def get_chat_name(self):
          date_str = self.date.strftime("%Y%m%d")
          return f"{date_str}-{self.company}-{self.work_type}"
      
      def total_cost(self, hours):
          return sum(emp.hourly_rate * hours for emp in self.employees)
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль ООП — часть 1

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day18_practice.py`:

```python
# Задача 1: Класс Employee (полный)
# Атрибуты: id, name, phone, hourly_rate, skills
# Методы: calculate_salary(hours), add_skill(skill), has_skill(skill)

# Задача 2: Класс Event (полный)
# Атрибуты: id, date, company, work_type, employees
# Методы: add_employee, remove_employee, get_chat_name, total_cost

# Задача 3: Класс WorkLog (учёт часов)
# Атрибуты: employee_id, event_id, hours, date
# Метод: calculate_payment(hourly_rate)

# Задача 4: Связь классов
# Создай Event, добавь 3 Employee
# Посчитай общую стоимость за 8 часов

# Задача 5: Список мероприятий
# Создай 3 Event, выведи названия чатов для каждого
```

### 13:45–14:00 | Итоги дня
- [ ] Класс = данные + поведение
- [ ] Твои будущие классы: Employee, Event, WorkLog

---

## 📅 День 19 — ООП: продвинутые темы

### Блок 1 (9:00–10:00) | Теория: Наследование
- [ ] Базовый и производный класс:
  ```python
  class Person:
      def __init__(self, name, phone):
          self.name = name
          self.phone = phone
  
  class Employee(Person):
      def __init__(self, name, phone, hourly_rate):
          super().__init__(name, phone)
          self.hourly_rate = hourly_rate
  ```
- [ ] `super()` — вызов метода родителя
- [ ] Переопределение методов

### Блок 2 (10:15–11:15) | Теория: Dataclasses
- [ ] Упрощённое создание классов:
  ```python
  from dataclasses import dataclass, field
  from datetime import date
  
  @dataclass
  class Employee:
      id: int
      name: str
      phone: str
      hourly_rate: float
      skills: list = field(default_factory=list)
  
  emp = Employee(1, "Иван", "+79991234567", 300)
  ```
- [ ] Автоматически создаются: `__init__`, `__repr__`, `__eq__`
- [ ] Меньше кода, меньше ошибок!

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль ООП — часть 2
- [ ] Codewars: задачи на классы

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day19_practice.py`:

```python
# Задача 1: Dataclass Employee
# Перепиши Employee как dataclass

# Задача 2: Dataclass Event
# Перепиши Event как dataclass

# Задача 3: Сериализация в JSON
# Метод to_dict() — возвращает словарь для JSON
# @classmethod from_dict(cls, data) — создаёт из словаря

# Задача 4: Менеджер данных
# class DataManager:
#     def save(self, filename)
#     def load(self, filename)
#     def add_employee(self, employee)
#     def add_event(self, event)

# Задача 5: Поиск и фильтрация
# Добавь методы:
# find_employee_by_name(name)
# find_employees_by_skill(skill)
# get_events_by_company(company)
```

### 13:45–14:00 | Итоги дня
- [ ] Dataclasses — современный способ создавать классы данных
- [ ] to_dict / from_dict — для сохранения в JSON

---

## 📅 День 20 — Обработка ошибок

### Блок 1 (9:00–10:00) | Теория: try-except
- [ ] Базовая обработка:
  ```python
  try:
      value = int(input("Число: "))
      result = 10 / value
  except ValueError:
      print("Это не число!")
  except ZeroDivisionError:
      print("Деление на ноль!")
  except Exception as e:
      print(f"Ошибка: {e}")
  ```
- [ ] Полная конструкция:
  ```python
  try:
      # опасный код
  except SomeError:
      # обработка ошибки
  else:
      # если ошибки НЕ было
  finally:
      # выполнится ВСЕГДА (очистка)
  ```

### Блок 2 (10:15–11:15) | Теория: raise и свои исключения
- [ ] Вызов исключения:
  ```python
  def set_hourly_rate(rate):
      if rate <= 0:
          raise ValueError("Ставка должна быть положительной")
      return rate
  ```
- [ ] Свои исключения:
  ```python
  class EmployeeNotFoundError(Exception):
      pass
  
  class InvalidDataError(Exception):
      pass
  
  # Использование
  def get_employee(id):
      emp = find_by_id(id)
      if emp is None:
          raise EmployeeNotFoundError(f"Сотрудник {id} не найден")
      return emp
  ```

### Блок 3 (11:30–12:30) | Практика: Stepik
- [ ] Stepik: модуль исключений

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day20_practice.py`:

```python
# Задача 1: Безопасный ввод числа
# Функция safe_input_int(prompt) — запрашивает, пока не введут число

# Задача 2: Безопасная загрузка JSON
# Если файла нет — вернуть пустой список
# Если JSON битый — сообщить и вернуть пустой список

# Задача 3: Валидация Employee
# Проверь: имя не пустое, телефон начинается с +7, ставка > 0
# Бросай InvalidDataError с понятным сообщением

# Задача 4: Валидация Event
# Проверь: дата в будущем, компания не пустая

# Задача 5: Обёртка для операций
# def safe_execute(func, *args, default=None):
#     try: return func(*args)
#     except: return default
```

### 13:45–14:00 | Итоги дня
- [ ] Обрабатывай только ожидаемые ошибки
- [ ] Свои исключения делают код понятнее

---

## 📅 День 21 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: Файлы и JSON
- [ ] Шпаргалка: open, read, write, json.dump, json.load
- [ ] Тест: сохрани список словарей в JSON, загрузи обратно

### Блок 2 (10:15–11:15) | Повторение: ООП
- [ ] Написать по памяти: dataclass с 5 полями
- [ ] Методы: to_dict, from_dict

### Блок 3 (11:30–12:30) | Повторение: datetime и исключения
- [ ] Форматы дат: strftime, strptime
- [ ] try-except-else-finally

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 3

## Задача: «Менеджер мероприятий (консоль + файлы)»

Объедини всё изученное за 3 недели!

**Классы (dataclass):**
```python
@dataclass
class Employee:
    id: int
    name: str
    phone: str
    hourly_rate: float
    skills: list

@dataclass
class Event:
    id: int
    date: date
    company: str
    work_type: str
    employee_ids: list  # ID назначенных сотрудников

@dataclass  
class WorkLog:
    id: int
    event_id: int
    employee_id: int
    hours: float
```

**Функционал:**

1. **Сотрудники:**
   - CRUD операции
   - Поиск по навыку
   - Сохранение в `employees.json`

2. **Мероприятия:**
   - CRUD операции
   - Назначить сотрудника на мероприятие
   - Генерация названия чата: `20250116-МузСервис-погрузка`
   - Сохранение в `events.json`

3. **Учёт часов:**
   - Добавить запись: сотрудник X отработал Y часов на мероприятии Z
   - Сохранение в `worklogs.json`

4. **Отчёты:**
   - Зарплата сотрудника за период
   - Стоимость мероприятия
   - Статистика по компаниям

**Требования:**
- Все данные в JSON-файлах
- Валидация данных с исключениями
- Минимум 3 класса + класс DataManager
- Консольное меню

**Структура проекта:**
```
event_manager/
├── models.py      # классы Employee, Event, WorkLog
├── storage.py     # класс DataManager (сохранение/загрузка)
├── validators.py  # функции валидации
└── main.py        # меню и логика
```

---

# НЕДЕЛЯ 4: Базы данных, Telegram, Excel, Веб

> **Цель недели:** Добавить SQLite, Telegram API, Excel, веб-интерфейс

---

## 📅 День 22 — SQLite: основы

### Блок 1 (9:00–10:00) | Теория: Введение в SQL
- [ ] Почему база данных лучше JSON:
  - Быстрый поиск по любому полю
  - Связи между таблицами
  - Надёжность (транзакции)
- [ ] SQLite — файловая БД, не нужен сервер
- [ ] Основные команды SQL:
  ```sql
  CREATE TABLE employees (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      name TEXT NOT NULL,
      phone TEXT,
      hourly_rate REAL
  );
  
  INSERT INTO employees (name, phone, hourly_rate) 
  VALUES ('Иван Петров', '+79991234567', 300);
  
  SELECT * FROM employees;
  SELECT * FROM employees WHERE hourly_rate > 250;
  
  UPDATE employees SET hourly_rate = 350 WHERE id = 1;
  
  DELETE FROM employees WHERE id = 1;
  ```

### Блок 2 (10:15–11:15) | Теория: Python + SQLite
- [ ] Работа с sqlite3:
  ```python
  import sqlite3
  
  # Подключение (создаст файл, если нет)
  conn = sqlite3.connect("events.db")
  cursor = conn.cursor()
  
  # Создание таблицы
  cursor.execute("""
      CREATE TABLE IF NOT EXISTS employees (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          name TEXT NOT NULL,
          phone TEXT,
          hourly_rate REAL
      )
  """)
  
  # Вставка
  cursor.execute(
      "INSERT INTO employees (name, phone, hourly_rate) VALUES (?, ?, ?)",
      ("Иван Петров", "+79991234567", 300)
  )
  conn.commit()
  
  # Выборка
  cursor.execute("SELECT * FROM employees")
  rows = cursor.fetchall()  # список кортежей
  
  conn.close()
  ```

### Блок 3 (11:30–12:30) | Практика: Создание БД
- [ ] Создай базу данных `events.db`
- [ ] Таблицы: employees, events, worklogs
- [ ] Добавь тестовые данные

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day22_practice.py`:

```python
# Задача 1: Создание таблиц
# employees: id, name, phone, hourly_rate
# events: id, date, company, work_type
# worklogs: id, event_id, employee_id, hours

# Задача 2: CRUD для employees
# add_employee(name, phone, rate)
# get_all_employees()
# get_employee_by_id(id)
# update_employee(id, **kwargs)
# delete_employee(id)

# Задача 3: Поиск
# find_employees_by_name(query) — поиск по части имени (LIKE)

# Задача 4: Связь таблиц
# Добавь employee_ids в events или создай таблицу event_employees

# Задача 5: Класс Database
# Обёртка над sqlite3 с методами для всех таблиц
```

### 13:45–14:00 | Итоги дня
- [ ] SQLite — простая, но мощная БД
- [ ] Используй `?` для параметров (защита от SQL-инъекций)

---

## 📅 День 23 — SQLite: продвинутые запросы

### Блок 1 (9:00–10:00) | Теория: JOIN и агрегации
- [ ] JOIN — связь таблиц:
  ```sql
  -- Получить часы с именами сотрудников
  SELECT e.name, w.hours, w.event_id
  FROM worklogs w
  JOIN employees e ON w.employee_id = e.id;
  
  -- Получить мероприятия с компанией
  SELECT * FROM events WHERE company = 'МузСервис';
  ```
- [ ] Агрегатные функции:
  ```sql
  SELECT COUNT(*) FROM employees;
  SELECT AVG(hourly_rate) FROM employees;
  SELECT SUM(hours) FROM worklogs WHERE employee_id = 1;
  SELECT company, COUNT(*) FROM events GROUP BY company;
  ```

### Блок 2 (10:15–11:15) | Теория: Расчёт зарплаты
- [ ] Сложный запрос — зарплата сотрудника:
  ```sql
  SELECT 
      e.name,
      SUM(w.hours) as total_hours,
      e.hourly_rate,
      SUM(w.hours) * e.hourly_rate as total_salary
  FROM worklogs w
  JOIN employees e ON w.employee_id = e.id
  WHERE w.event_id IN (SELECT id FROM events WHERE date BETWEEN ? AND ?)
  GROUP BY e.id;
  ```
- [ ] В Python:
  ```python
  def get_salary_report(start_date, end_date):
      cursor.execute("""...""", (start_date, end_date))
      return cursor.fetchall()
  ```

### Блок 3 (11:30–12:30) | Практика: Отчёты
- [ ] Реализуй запросы для отчётов
- [ ] Тестируй с разными данными

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day23_practice.py`:

```python
# Задача 1: Отчёт по сотруднику
# get_employee_report(employee_id, start_date, end_date)
# → часы, заработок, список мероприятий

# Задача 2: Отчёт по мероприятию
# get_event_report(event_id)
# → список сотрудников, часы каждого, общая стоимость

# Задача 3: Отчёт по компании
# get_company_report(company)
# → все мероприятия, общие расходы

# Задача 4: Топ сотрудников
# get_top_employees(limit=5)
# → по количеству отработанных часов

# Задача 5: Класс ReportGenerator
# Объедини все отчёты в один класс
```

### 13:45–14:00 | Итоги дня
- [ ] JOIN — ключ к связанным данным
- [ ] GROUP BY + агрегаты = мощные отчёты

---

## 📅 День 24 — Telegram API: создание чатов

### Блок 1 (9:00–10:00) | Теория: Telegram Bot API vs User API
- [ ] Два способа работы с Telegram:
  - **Bot API** — через бота (ограничения: бот не может создавать группы)
  - **User API** — от имени пользователя (можно всё)
- [ ] Для создания чатов нужен User API
- [ ] Библиотека Pyrogram:
  ```bash
  pip install pyrogram tgcrypto
  ```
- [ ] Получение API credentials:
  1. Зайди на my.telegram.org
  2. API Development Tools
  3. Получи `api_id` и `api_hash`

### Блок 2 (10:15–11:15) | Теория: Pyrogram — основы
- [ ] Авторизация:
  ```python
  from pyrogram import Client
  
  # Первый запуск — попросит номер телефона и код
  app = Client("my_account", api_id=12345, api_hash="abc123...")
  
  async def main():
      async with app:
          me = await app.get_me()
          print(f"Авторизован как {me.first_name}")
  
  app.run(main())
  ```
- [ ] Создание группы:
  ```python
  async def create_event_chat(name, user_ids):
      async with app:
          chat = await app.create_group(name, user_ids)
          return chat.id
  ```

### Блок 3 (11:30–12:30) | Практика: Настройка Pyrogram
- [ ] Установи pyrogram
- [ ] Получи api_id и api_hash
- [ ] Авторизуйся (создастся файл сессии)
- [ ] Отправь тестовое сообщение себе

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day24_practice.py`:

```python
# Задача 1: Авторизация
# Создай клиент, авторизуйся, выведи своё имя

# Задача 2: Создание группы
# async def create_group(name) → chat_id

# Задача 3: Добавление пользователей
# async def add_users_to_chat(chat_id, usernames)

# Задача 4: Отправка сообщения
# async def send_welcome_message(chat_id, event_info)

# Задача 5: Класс TelegramManager
# create_event_chat(event) — создаёт чат с правильным названием
# add_employees(chat_id, employees) — добавляет по username
# send_info(chat_id, text) — отправляет сообщение
```

### 13:45–14:00 | Итоги дня
- [ ] Pyrogram — мощная библиотека для Telegram
- [ ] Храни api_id/api_hash в .env файле!

---

## 📅 День 25 — Работа с Excel (openpyxl)

### Блок 1 (9:00–10:00) | Теория: Чтение Excel
- [ ] Установка: `pip install openpyxl`
- [ ] Чтение файла:
  ```python
  from openpyxl import load_workbook
  
  wb = load_workbook("template.xlsx")
  ws = wb.active  # активный лист
  
  # Чтение ячейки
  value = ws["A1"].value
  value = ws.cell(row=1, column=1).value
  
  # Итерация по строкам
  for row in ws.iter_rows(min_row=2, values_only=True):
      print(row)  # кортеж значений
  ```

### Блок 2 (10:15–11:15) | Теория: Создание и редактирование
- [ ] Создание файла:
  ```python
  from openpyxl import Workbook
  
  wb = Workbook()
  ws = wb.active
  ws.title = "Сотрудники"
  
  # Запись
  ws["A1"] = "Имя"
  ws["B1"] = "Ставка"
  ws.append(["Иван Петров", 300])
  
  wb.save("output.xlsx")
  ```
- [ ] Заполнение шаблона:
  ```python
  wb = load_workbook("contract_template.xlsx")
  ws = wb.active
  
  ws["B5"] = "Иванов Иван Иванович"
  ws["B6"] = "01.02.2025"
  ws["B10"] = 2400  # сумма
  
  wb.save("contract_ivanov.xlsx")
  ```

### Блок 3 (11:30–12:30) | Практика: Работа с шаблонами
- [ ] Создай шаблон договора в Excel
- [ ] Заполни его из Python
- [ ] Создай шаблон акта выполненных работ

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day25_practice.py`:

```python
# Задача 1: Экспорт сотрудников в Excel
# export_employees(employees, filename)

# Задача 2: Экспорт мероприятий
# export_events(events, filename)

# Задача 3: Генерация договора
# generate_contract(employee, event, template_path, output_path)
# Заполни: ФИО, дата, компания, вид работ, ставка

# Задача 4: Генерация акта
# generate_act(employee, event, hours, template_path, output_path)
# Заполни: ФИО, мероприятие, часы, сумма

# Задача 5: Класс DocumentGenerator
# generate_contract(employee, event) → filepath
# generate_act(employee, event, hours) → filepath
```

### 13:45–14:00 | Итоги дня
- [ ] openpyxl — стандарт для работы с Excel в Python
- [ ] Шаблоны документов экономят кучу времени

---

## 📅 День 26 — Streamlit: веб-интерфейс

### Блок 1 (9:00–10:00) | Теория: Основы Streamlit
- [ ] Установка: `pip install streamlit`
- [ ] Запуск: `streamlit run app.py`
- [ ] Базовые элементы:
  ```python
  import streamlit as st
  
  st.title("Менеджер мероприятий")
  st.header("Сотрудники")
  
  name = st.text_input("Имя")
  rate = st.number_input("Ставка", min_value=0)
  
  if st.button("Добавить"):
      st.success(f"Добавлен {name}")
  ```

### Блок 2 (10:15–11:15) | Теория: Таблицы и формы
- [ ] Отображение данных:
  ```python
  import pandas as pd
  
  # Таблица
  df = pd.DataFrame(employees)
  st.dataframe(df)
  
  # Редактируемая таблица
  edited = st.data_editor(df)
  ```
- [ ] Sidebar и страницы:
  ```python
  page = st.sidebar.selectbox("Страница", ["Сотрудники", "Мероприятия"])
  
  if page == "Сотрудники":
      show_employees()
  else:
      show_events()
  ```
- [ ] Состояние (session_state):
  ```python
  if "employees" not in st.session_state:
      st.session_state.employees = load_from_db()
  ```

### Блок 3 (11:30–12:30) | Практика: Первое приложение
- [ ] Создай app.py с меню
- [ ] Страница "Сотрудники" — список + форма добавления

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day26_practice.py`:

```python
# Задача 1: Страница сотрудников
# - Таблица всех сотрудников
# - Форма добавления
# - Кнопка удаления

# Задача 2: Страница мероприятий
# - Список мероприятий
# - Форма создания (дата, компания, вид работ)
# - Генерация названия чата

# Задача 3: Назначение на мероприятие
# - Выбор мероприятия
# - Мультиселект сотрудников
# - Кнопка "Создать чат в Telegram"

# Задача 4: Учёт часов
# - Выбор мероприятия и сотрудника
# - Ввод часов
# - Автоматический расчёт суммы

# Задача 5: Отчёты
# - Выбор периода
# - Таблица: сотрудник, часы, сумма
# - Кнопка "Экспорт в Excel"
```

### 13:45–14:00 | Итоги дня
- [ ] Streamlit — самый быстрый способ сделать веб-интерфейс
- [ ] session_state для хранения данных между перезагрузками

---

## 📅 День 27 — Streamlit: продвинутые возможности

### Блок 1 (9:00–10:00) | Теория: Многостраничное приложение
- [ ] Структура:
  ```
  app/
  ├── app.py              # главная
  └── pages/
      ├── 1_Сотрудники.py
      ├── 2_Мероприятия.py
      ├── 3_Часы.py
      └── 4_Отчёты.py
  ```
- [ ] Общие данные через модуль:
  ```python
  # database.py
  from db import Database
  db = Database("events.db")
  
  # pages/1_Сотрудники.py
  from database import db
  employees = db.get_all_employees()
  ```

### Блок 2 (10:15–11:15) | Теория: Интеграция всех компонентов
- [ ] Связь: Streamlit → SQLite → Excel → Telegram
  ```python
  # При нажатии "Создать мероприятие"
  if st.button("Создать"):
      # 1. Сохранить в БД
      event_id = db.add_event(date, company, work_type)
      
      # 2. Генерировать название чата
      chat_name = f"{date.strftime('%Y%m%d')}-{company}-{work_type}"
      
      # 3. Создать чат в Telegram (если выбрано)
      if create_chat:
          chat_id = telegram.create_group(chat_name, employee_phones)
          db.update_event(event_id, chat_id=chat_id)
      
      st.success(f"Создано: {chat_name}")
  ```

### Блок 3 (11:30–12:30) | Практика: Сборка приложения
- [ ] Объедини все страницы
- [ ] Протестируй полный цикл:
  1. Добавить сотрудника
  2. Создать мероприятие
  3. Назначить сотрудников
  4. Записать часы
  5. Выгрузить отчёт

### Блок 4 (12:45–13:45) | Практика: Самостоятельные задачи
Файл `day27_practice.py`:

```python
# Задача 1: Страница документов
# - Выбор сотрудника и мероприятия
# - Кнопки: "Сгенерировать договор", "Сгенерировать акт"
# - Скачивание файла: st.download_button

# Задача 2: Дашборд (главная страница)
# - Метрики: всего сотрудников, мероприятий, часов
# - Ближайшие мероприятия
# - Последние записи часов

# Задача 3: Поиск и фильтрация
# - Поиск сотрудника по имени
# - Фильтр мероприятий по компании и дате

# Задача 4: Настройки
# - Редактирование шаблонов документов
# - Настройки Telegram (вкл/выкл)

# Задача 5: Резервное копирование
# - Кнопка "Экспорт всех данных" (JSON)
# - Загрузка данных из JSON
```

### 13:45–14:00 | Итоги дня
- [ ] Многостраничные приложения — удобно и профессионально
- [ ] st.download_button — для скачивания сгенерированных файлов

---

## 📅 День 28 — Повторение + СЛОЖНАЯ ЗАДАЧА

### Блок 1 (9:00–10:00) | Повторение: SQLite
- [ ] CRUD операции
- [ ] JOIN и агрегатные функции
- [ ] Параметризованные запросы

### Блок 2 (10:15–11:15) | Повторение: Telegram + Excel
- [ ] Pyrogram: создание чата, добавление пользователей
- [ ] openpyxl: чтение, запись, шаблоны

### Блок 3 (11:30–12:30) | Повторение: Streamlit
- [ ] Основные элементы
- [ ] Многостраничность
- [ ] session_state

### Блок 4 (12:45–13:45) | 🎯 СЛОЖНАЯ ЗАДАЧА НЕДЕЛИ 4

## Задача: «Рабочий прототип»

Собери всё вместе в работающее приложение!

**Минимальный функционал:**

1. **Сотрудники:**
   - Просмотр списка
   - Добавление (имя, телефон, ставка)
   - Удаление

2. **Мероприятия:**
   - Просмотр списка
   - Создание (дата, компания, вид работ)
   - Автогенерация названия чата

3. **Назначение:**
   - Выбрать мероприятие
   - Выбрать сотрудников
   - Кнопка "Создать чат в Telegram" (опционально, если настроен)

4. **Учёт часов:**
   - Выбор мероприятия и сотрудника
   - Ввод часов
   - Показ суммы

5. **Документы:**
   - Генерация договора (Excel)
   - Генерация акта (Excel)
   - Скачивание файлов

**Структура:**
```
event_manager/
├── app.py
├── pages/
│   ├── 1_👥_Сотрудники.py
│   ├── 2_📅_Мероприятия.py
│   ├── 3_⏱_Часы.py
│   └── 4_📄_Документы.py
├── database.py
├── telegram_manager.py
├── document_generator.py
├── templates/
│   ├── contract_template.xlsx
│   └── act_template.xlsx
└── events.db
```

**Критерии:**
- ✅ Приложение запускается
- ✅ Можно добавить сотрудника
- ✅ Можно создать мероприятие
- ✅ Генерируется название чата в формате "20250116-МузСервис-погрузка"
- ✅ Можно записать часы
- ✅ Генерируется Excel-документ

---

# ФИНАЛ: Дни 29-31

---

## 📅 День 29 — Планирование и архитектура

### 9:00–14:00 | Финальное планирование

1. **Ревью задачи недели 4** (1 час)
   - Что работает?
   - Что нужно доделать?
   - Какие баги?

2. **Список фич для MVP** (1 час)
   - Must have (обязательно)
   - Nice to have (желательно)
   - Later (потом)

3. **Рефакторинг кода** (2 часа)
   - Структура файлов
   - Убрать дублирование
   - Добавить комментарии

4. **Тестирование** (1 час)
   - Проверить все сценарии
   - Найти и записать баги

---

## 📅 День 30 — Разработка

### 9:00–14:00 | Кодинг!

**Приоритеты:**
1. Исправить критические баги
2. Доделать основной функционал
3. Улучшить UI/UX
4. Добавить обработку ошибок

**Чеклист:**
- [ ] Все CRUD работают
- [ ] Название чата генерируется правильно
- [ ] Часы считаются корректно
- [ ] Документы генерируются
- [ ] Нет падений при неправильном вводе

---

## 📅 День 31 — Финализация и презентация

### Блок 1-2 (9:00–11:15) | Доработка
- [ ] Последние исправления
- [ ] Тестирование
- [ ] Очистка кода

### Блок 3 (11:30–12:30) | Документация
- [ ] README.md:
  ```markdown
  # Менеджер мероприятий
  
  ## Установка
  pip install -r requirements.txt
  
  ## Запуск
  streamlit run app.py
  
  ## Функционал
  - Управление сотрудниками
  - Управление мероприятиями
  - Учёт рабочих часов
  - Генерация документов
  - Интеграция с Telegram
  ```
- [ ] requirements.txt
- [ ] Скриншоты

### Блок 4 (12:45–13:45) | Презентация
- [ ] Запиши короткое демо (можно скринкаст)
- [ ] Или подготовь скриншоты ключевых экранов
- [ ] Отправь мне для проверки!

---

# 🎯 ФИНАЛЬНЫЙ ПРОЕКТ

## «Менеджер мероприятий»

### Функционал MVP:

| Модуль | Функции |
|--------|---------|
| 👥 Сотрудники | CRUD, поиск, список навыков |
| 📅 Мероприятия | CRUD, дата, компания, вид работ |
| 🔗 Назначение | Привязка сотрудников к ивенту |
| 💬 Telegram | Создание чата "20250116-Компания-работа" |
| ⏱ Учёт часов | Запись часов, расчёт оплаты |
| 📄 Документы | Договор и акт из Excel-шаблона |
| 📊 Отчёты | По сотруднику, по мероприятию |

### Технологии:
- Python 3.12
- Streamlit (веб-интерфейс)
- SQLite (база данных)
- Pyrogram (Telegram)
- openpyxl (Excel)

### Критерии оценки:
- ✅ Приложение запускается без ошибок
- ✅ Можно вести базу сотрудников
- ✅ Можно создавать мероприятия
- ✅ Название чата в формате ГГГГММДД-Компания-работа
- ✅ Учёт часов работает
- ✅ Документы генерируются
- ✅ Код структурирован (модули, классы)
- ✅ Есть обработка ошибок
- ✅ Есть README

---

# 📝 Чек-лист прогресса

## Неделя 1: Основы
- [ ] День 1: Переменные, типы
- [ ] День 2: Операторы
- [ ] День 3: Условия
- [ ] День 4: while
- [ ] День 5: for
- [ ] День 6: Строки
- [ ] День 7: ✅ Задача «Валидатор паролей»

## Неделя 2: Структуры данных
- [ ] День 8: Списки (основы)
- [ ] День 9: Списки (comprehension)
- [ ] День 10: Кортежи, множества
- [ ] День 11: Словари
- [ ] День 12: Функции (основы)
- [ ] День 13: Функции (продвинутые)
- [ ] День 14: ✅ Задача «База сотрудников»

## Неделя 3: Файлы, модули, ООП
- [ ] День 15: Файлы
- [ ] День 16: JSON
- [ ] День 17: Модули, datetime
- [ ] День 18: ООП (классы)
- [ ] День 19: ООП (dataclasses)
- [ ] День 20: Исключения
- [ ] День 21: ✅ Задача «Менеджер мероприятий (консоль)»

## Неделя 4: БД, Telegram, Excel, Веб
- [ ] День 22: SQLite (основы)
- [ ] День 23: SQLite (запросы)
- [ ] День 24: Telegram API
- [ ] День 25: Excel (openpyxl)
- [ ] День 26: Streamlit (основы)
- [ ] День 27: Streamlit (продвинутое)
- [ ] День 28: ✅ Задача «Рабочий прототип»

## Финал
- [ ] День 29: Планирование
- [ ] День 30: Разработка
- [ ] День 31: ✅ Финальный проект

---
